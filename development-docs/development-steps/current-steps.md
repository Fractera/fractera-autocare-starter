# current-steps.md — состояние проекта между сессиями

## Зачем этот файл существует

**Фича живёт дольше сессии.** От начала разработки до репорта проходит не одна сессия, а иногда
десятки. Каждая новая начинается с пустой головы, и техническое задание шага 6, 8 или 28 ей не
поможет: задание говорит, что задумано, и молчит о том, **что уже сделано и на что смотреть**.

**Поэтому здесь не перечисление шагов, а состояние проекта.** Прочитав только этот файл, агент входит
в разработку сразу — не восстанавливая вчерашний день по коммитам, отчётам и памяти, которой у него
нет.

🔒 **ЭТО НАША ЗАМЕНА `/compact` — И ЗАМЕНА ОБЯЗАТЕЛЬНАЯ** (решение владельца 2026-08-22).
`/compact` сжимает контекст моделью: что именно потеряно, не видит ни агент, ни человек, и пропажа
всплывает через час — забытым решением владельца или законом, который «был в начале сессии».
Здесь состояние записано **буквами, в файл**: его можно прочитать, проверить и поправить, и оно
переживает не только сброс, но и смену модели.

**Один файл на весь проект.** Переписывается ПОВЕРХ, а не дописывается. Всё, что уже стало итогом,
отсюда уходит в `completed-steps/`.

**Имя во множественном числе намеренно:** работа почти никогда не один шаг. Это группа связанных шагов
и подшагов, и половина смысла файла — держать их связи и условия закрытия, которых нет больше нигде.

**Читается ПЕРВЫМ в начале КАЖДОЙ сессии**, раньше плана шага и раньше кода.

---

## Сейчас в работе

**Группа «Переезд Автозаботы на Fractera».** Начата 2026-08-24. Шаги 1–5 закрыты.
🟢 **Скелет определён:** тип `scheduler`, четыре роли, поток от гостя до администратора.
🟢 **Фаза исследования закрыта:** тип, авторизация и роли определены, блокирующих вопросов нет.
⛔ **Ни одна строка этого скелета не построена** — это карта работы, а не отчёт о ней.

| Раздел | Состояние |
|---|---|
| **Группа** | перенести продукт «Автозабота» (исходник Филиппа `carecrm`) на архитектуру Fractera. Начата 2026-08-24 |
| **Шаги** | **1 — инвентаризация · ЗАКРЫТ**. **2 — паспорт · ЗАКРЫТ**. **3 — страница паспорта · ЗАКРЫТ**. **4 — первое обновление сервера · ЗАКРЫТ** (3 пруфа из 4). **5 — тип приложения и роли · ЗАКРЫТ**. **6 — не заведён** |
| **Условие закрытия группы** | таймерные цепочки работают на своём сервере со своей базой. 🔒 Мера успеха двойная: уведомление получают И те, кто попал в мгновенный триггер, И те, кто попал в рассылку по расписанию |
| **Сделано и чем доказано** | ① карта исходника. ② паспорт. ③ CRM = **YCLIENTS**, четыре следа. ④ страница паспорта: чужая роль `403 {"requires":["architect"]}`. ⑤ **сервер обновлён:** `commit` = `3b24467` (было `null`), `status.sh` = 0, uptime 8760→12299; гость получает 200 + `401` и окно `AccessGate`. ⑥ **скелет определён:** права в исходнике измерены — 8 мест с `role !== "admin"` при негативном контроле 0 по задачам; роли уже существуют в `lib/roles.ts`, `admin` в `staff` не входит |
| **Решения владельца** | 🔒 **тип приложения — `scheduler`** · 🔒 **роли: `user`, `vip_user`, `manager`, `admin`** (`operator → manager`) · 🔒 **главная: бейджи «Автозабота», название компании, одна кнопка «Войти», больше ничего** · 🔒 закон старта · 🔒 наличие ≠ задание · 🔒 отправка ChatPush, временно · 🔒 мера успеха — оба пути · 🔒 цепочку будят схемы автоматизаций, скрипт создаёт ИИ · 🔒 одно приложение — одно учреждение · 🔒 регулятора временно не оптимизируем · 🔒 сначала создать, потом удалять. Все дословно в `PASSPORT.md` |
| **Чего НЕ делать** | ① не выводить задание из наличия папки в шаблоне. ② не переносить факт об исходнике как факт о проекте — **и обратно тоже**: отсутствие сущности в исходнике не означает её отсутствия в продукте (так я ошибочно списал группу `(account)`). ③ не измерять право на машине с обходом (`NODE_ENV=development` → всем `architect`; чужая роль — заголовком `x-agent-identity`). ④ не округлять частичный ответ владельца до ✅. ⑤ исключение из правила блоков — только на странице паспорта. ⑥ **прогонять `npm run prebuild` целиком**, а не выбранный список. ⑦ **не «чинить» `check:config-schemas` и `check:blocks-map` на Windows** — это CRLF. ⑧ **не печатать `git remote -v` сервера** — в адресе живой токен. ⑨ **главная переделывается списком блоков и видимостью, не `rm`** — удалять построенное запрещено. ⑩ `clinic_id` не переезжает |
| **Следующее действие** | Рома называет шаг 6. Фаза исследования закрыта: тип, авторизация и роли определены, блокирующих вопросов нет |

### Что открыто

✅ **Три вопроса шага 5 закрыты Ромой 2026-08-24** — записаны дословно в `PASSPORT.md` §3.36.
Права администратора: **вариант B**, `lib/roles.ts` не трогаем, работы ноль. Регистрация: своей
страницы не строим, выбор живёт в окне авторизации платформы. VIP назначает оператор.

- 🛑 **Два вопроса про экран VIP — поднять В МОМЕНТ ЕГО ПОСТРОЙКИ, не раньше и не молча.**
  ① Роль без учётной записи входа не даёт: либо запись внутри приложения без входа (и роль не нужна),
  либо в слое авторизации заводится учётная запись — вопрос самого Ромы. ② Проверкой найдено: слова
  «механизм переносится из исходника» не подтвердились — описанной кнопки там нет, есть две разные
  («Добавить пациента» — обеим ролям, роли не назначает; «Добавить сотрудника» — только админу,
  назначает лишь `admin`/`operator`), а роли `vip` в исходнике нет вовсе. Значит это новая постройка,
  и она **расширяет право**: роли назначал админ, будет оператор — нужен противовес (`use-roles` §4);
- 🔴 **перевыпустить токен GitHub** — лежит открытым в `/opt/fractera/app/.git/config` и попал в
  переписку;
- **четвёртый пруф шага 4** — «архитектор видит паспорт»: нужна сессия Ромы;
- **четыре предложения по навыкам** ждут слова: `use-roles` §5 (право на машине с обходом),
  `use-deploy` §5 (прогонять весь prebuild), запись про `previews.generated`, и строка в `CLAUDE.md`
  про каталог 22 направлений как инструмент выбора типа продукта.

### Как теперь устроен канал на сервер

`.env.local` из панельной выгрузки, ключ в `.fractera-ssh/fractera-agent-key` (папка под `.gitignore`).
Собирает **панель** (`FRACTERA_DEPLOY_SECRET` есть). Команда одна: `bash scripts/server/deploy.sh <пути>`.
🔒 **`git push` идёт только токеном из `.env.local`** — окружение аутентифицируется как `bolshiyanov`,
прав у него нет (403). 🔒 **Слот подтягивает GitHub сам.**

### Открытые вопросы к ядру — к своему шагу, не к старту

Полностью — `PASSPORT.md` §9. Ближайшие: где живёт фоновый процесс автоматизаций и чем запускается на
Ubuntu · что делать со старым скриптом при повторной генерации ИИ · как хранится и валидируется «схема
автоматизации» · словарь предметной области · заводится ли продукт в панели и под каким `id`.

---

## Форма записи

Разделы выше — и есть форма; пустые не удалять, пустой раздел это тоже ответ.

| Раздел | Что в нём |
|---|---|
| **Группа** | одной фразой, что строим целиком, и когда начали |
| **Таблица шагов** | номер · что · состояние · **условие закрытия** |
| **Попутные шаги** | те, которых не планировали, — с причиной появления |
| **Условие закрытия группы** | что должно быть верно, чтобы вся группа считалась законченной |
| **Сделано и чем доказано** | по строке на пункт: не «сделал форму», а «форма записана, хэши сошлись» |
| **Решения владельца** | дословно, не пересказ: пересказ теряет то, ради чего решение принято |
| **Чего НЕ делать** | тупики, уже оплаченные в этой группе |
| **Следующее действие** | одна фраза; список — это план, он живёт в файле шага |

---

## Когда этот файл обнуляется

**Агент обнуляет его сам, при закрытии группы, той же операцией.** Содержимое уходит двумя адресами:
итоги шагов — в `completed-steps/`, а картина целого — в **репорт фичи** (`development-docs/reports/`,
категория `feature-`). После этого здесь снова «пусто — работа не начата».

Файл, который не обнуляют, за месяц становится вторым журналом: его чтение само превращается в расход
контекста — ровно в тот, ради экономии которого он заведён.
