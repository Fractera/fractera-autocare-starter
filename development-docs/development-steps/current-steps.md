# current-steps.md — состояние проекта между сессиями

## Зачем этот файл существует

**Фича живёт дольше сессии.** От начала разработки до репорта проходит не одна сессия, а иногда
десятки. Каждая новая начинается с пустой головы, и техническое задание шага 6, 8 или 28 ей не
поможет: задание говорит, что задумано, и молчит о том, **что уже сделано и на что смотреть**.

**Поэтому здесь не перечисление шагов, а состояние проекта.** Прочитав только этот файл, агент входит
в разработку сразу — не восстанавливая вчерашний день по коммитам, отчётам и памяти, которой у него
нет.

🔒 **ЭТО НАША ЗАМЕНА `/compact` — И ЗАМЕНА ОБЯЗАТЕЛЬНАЯ** (решение владельца 2026-08-22).
`/compact` сжимает контекст моделью: что именно потеряно, не видит ни агент, ни человек, и пропажа
всплывает через час — забытым решением владельца или законом, который «был в начале сессии».
Здесь состояние записано **буквами, в файл**: его можно прочитать, проверить и поправить, и оно
переживает не только сброс, но и смену модели.

**Один файл на весь проект.** Переписывается ПОВЕРХ, а не дописывается. Всё, что уже стало итогом,
отсюда уходит в `completed-steps/`.

**Имя во множественном числе намеренно:** работа почти никогда не один шаг. Это группа связанных шагов
и подшагов, и половина смысла файла — держать их связи и условия закрытия, которых нет больше нигде.

**Читается ПЕРВЫМ в начале КАЖДОЙ сессии**, раньше плана шага и раньше кода.

---

## Сейчас в работе

**Группа «Переезд Автозаботы на Fractera».** Начата 2026-08-24. **Шаги 1–19 закрыты.**
🟢 **ТЕПЕРЬ ЗАГЛУШЕК ДЕЙСТВИТЕЛЬНО НЕТ — ДЕСЯТЬ АДРЕСОВ.** Строка «девять» была завышением: `/ru/company`
(«Данные компании») в девятку не входил и оставался заглушкой шага 7. Наполнен шагом 22 — форма
названия компании и контактного телефона, право `admin`.
🟢 Девять адресов показывают настоящие данные филиала или честно
объясняют, почему данных нет: `/ru/dashboard` · `/ru/patients` + карточка · `/ru/tasks` ·
`/ru/scenarios` · `/ru/services` · `/ru/messages` · `/ru/analytics` · `/ru/audit` · `/ru/clients`.
Одиннадцать дверей `/api/care/*`, все дают гостю `401` на сервере. Доставлено: `commit` = `0d90142`.
🟢 **СИСТЕМА РЕШАЕТ САМА (шаг 15).** Правило «не был 60 дней» нашло 94 человека и завело 94 задачи;
независимый контрольный запрос дал те же 94.
⛔ **ЯДРО НЕ ПОСТРОЕНО (шаг 20+):** нет двух таймеров, нет отправки ChatPush, нет приёма сообщений.
Условие закрытия группы не выполнено — цепочки не работают, потому что их некому будить и нечем
отправлять.
🟢 **ДАННЫЕ:** `LS centr`, Кисловодск, `916279` — 1844 человека, 6232 строки визитов, 129 услуг,
выручка 16 408 655, средний чек 2935.
🟢 **СОГЛАСИЕ ИЗМЕРЕНО (шаг 13).** 481 человек из 1844 отказались письменно; разрешений в CRM не
встретилось ни одного; у 1363 записи нет — по решению Ромы считаются разрешением. До шага 13 продукт
написал бы всем 1844.
🔴 **ТРИ ОТКРЫТЫХ ВОПРОСА К РОМЕ:**
① **цепочка `birthday` мертва** — даты рождения нет ни у кого, CRM их не хранит. Убирать триггер или
ждать, пока клиника начнёт записывать;
② **1535 прошедших визитов со статусом `2` («подтверждён») нигде не считаются состоявшимися** — ни в
последнем визите, ни в сегментах, ни в правилах. Если клиника отмечает явку двойкой, «последний визит»
занижен и правило «не был 60 дней» находит лишних;
③ **глазами не проверен ни один экран** — расширение Chrome отключено всю сессию.

| Раздел | Состояние |
|---|---|
| **Группа** | перенести продукт «Автозабота» (исходник Филиппа `carecrm`) на архитектуру Fractera. Начата 2026-08-24 |
| **Шаги** | **1–9 · ЗАКРЫТЫ** (инвентаризация · паспорт · страница паспорта · сервер · тип и роли · проектирование · скелет · уборка страниц · уборка меню). **10 — фундамент данных · ЗАКРЫТ** (подшаги 10-1, 10-2). **11 — чтение YCLIENTS · ЗАКРЫТ 2026-08-25** (подшаги 11-1, 11-2). **12 — экраны людей и аудита · ЗАКРЫТ 2026-08-25** (подшаги 12-1 … 12-4). **13 — согласие из CRM · ЗАКРЫТ 2026-08-25** (попутный, подшаги 13-1…13-3). **14 — «Задачи» · ЗАКРЫТ 2026-08-25** (подшаги 14-1…14-3). **15 — «Сценарии» · ЗАКРЫТ** · **16 — «Услуги» · ЗАКРЫТ** · **17 — «Переписка» · ЗАКРЫТ** · **18 — «Аналитика» · ЗАКРЫТ** · **19 — «Обзор» и «Клиенты» · ЗАКРЫТ** (попутный, в очереди их не было). **20 — ЯДРО: два таймера, отправка ChatPush, приём сообщений · не заведён**. **21 — единый LF · ЗАКРЫТ 2026-08-25** (попутный, аварийный: жалоба Ромы на боевой сайт вскрыла аддитивную доставку и два вечно красных гейта). **22 — «Данные компании», вход в карточку, демонстрация переписки · ЗАКРЫТ 2026-08-25** · **23 — дорога из карточки в чат и подсказка в ящике · ЗАКРЫТ** · **24 — обрезание длинного значения в плитке · ЗАКРЫТ** · **25 — ЧАТ НА AI ELEMENTS · ЗАКРЫТ 2026-08-25** · **26 — вложения по стандарту AI SDK, плеер, микрофон · ЗАКРЫТ** · **27 — главная: бейдж, мерцание, кнопка, логотип и фавикон · ЗАКРЫТ** · **28 — главная РАЗНАЯ по ролям + заявка стать клиентом · ЗАКРЫТ 2026-08-25** (три заказа Ромы одной сессией; развилку хранилища он решил в пользу своей двери в `APP-CONFIG`) |

| **Условие закрытия группы** | таймерные цепочки работают на своём сервере со своей базой. 🔒 Мера успеха двойная: уведомление получают И те, кто попал в мгновенный триггер, И те, кто попал в рассылку по расписанию |
| **Сделано и чем доказано** | ① карта исходника ② паспорт ③ CRM = **YCLIENTS** ④ страница паспорта: чужая роль `403` ⑤ сервер обновлён: `commit` = `3b24467` ⑥ тип `scheduler` и четыре роли ⑦ архитектура согласована (§11) ⑧ скелет: 14 адресов, 14 РАЗЛИЧНЫХ `<h1>` ⑨ **уборка:** семь удалённых адресов дают **404** при негативном контроле — **17 из 17** адресов дерева отвечают **200**; `prebuild` доходит до конца, 16 гейтов + типы + словари зелёные ⑩ **меню чистые:** 20 адресов мебели, живых 20, битых 0 при негативном контроле (`/ru/blog` и `/ru/products`, стоявшие там до шага 8, → 404); ноль упоминаний удалённого в разметке ⑪ **фундамент данных:** восемь таблиц отвечают запросу при негативном контроле `no such table`; частичный индекс проверен ПОВЕДЕНИЕМ (вторая открытая задача отклонена, после закрытия первой новая создалась); сегменты прогнаны на выдуманных данных — цикл 30 дней, просрочивший найден, ритмичные не задеты ⑫ **данные живого филиала в таблицах:** три прогона подряд дали одинаковые девять счётчиков (1844 / 1844 / 6232 / 129), строк ровно столько же, сколько различных пар (запись, услуга) — ни одного дубля; сверка с CRM сходится нацело: `1849 − 4 без телефона − 1 схлопнутая карточка = 1844`; негативный контроль на пробной таблице — `NULL` даёт две строки, `''` одну, разные услуги не схлопнуты ⑯ **девять экранов на живых данных:** одиннадцать дверей `/api/care/*` дают гостю `401` на СЕРВЕРЕ при негативном контроле `/api/health` → `200`; правило нашло 94 человека и независимый контрольный запрос дал те же 94; каталог 129 услуг, правка сдвинула ровно четыре числа сводки; аналитика сошлась с замерами шагов 11 и 12 ⑭
 **согласие измерено:** два прогона на одних и тех же 1844 карточках дали РАЗНОЕ распределение (357/1487 против 481/1363) — разница не в данных CRM, а в том, дочитали мы их или нет; после лечения непрочитанных `0`, и `481 + 1363 = 1844 = COUNT(*)` ⑮ **три предохранителя задач:** отказавшиеся → `noConsent 3`, повторно те же → `hasOpenTask 3`, после закрытия задачи → `recentlyContacted 1`; согласные в том же опыте → `created 3`, то есть ноль не от поломки вставки ⑬
 **экраны на живых данных:** три двери дают гостю `401` на СЕРВЕРЕ при негативном контроле `/api/health` → `200`; список сходится с базой (`total` 1844 = `COUNT(*)`), поиск проверен реальной подстрокой (SQL 2 = дверь 2, отброшено 1842, «ъыь» → 0), карточка отдаёт 10 строк на 9 записей CRM, небывалый id → `404`; аудит сошёлся со всеми независимыми замерами |
| **Решения владельца** | 🔒 тип **`scheduler`** · роли **`user`, `vip_user`, `manager`, `admin`** · главная: бейджи, название компании, кнопка «Войти» · вход **только по email** · после входа развилка **по роли** · кабинет — минимальная заглушка · продукт в `PRODUCTS-CONFIG` **не заводим** · права админа — **вариант B** · отправка **ChatPush, временно** · мера успеха — оба пути · цепочку будят схемы автоматизаций со скриптом от ИИ · одно приложение — одно учреждение · регулятора временно не оптимизируем · **сначала воспроизвести, потом удалять**. Все дословно в `PASSPORT.md` |
| **Чего НЕ делать** | ① не выводить задание из наличия папки в шаблоне ② не переносить факт об исходнике как факт о проекте — и обратно тоже ③ не измерять право на машине с обходом (`x-agent-identity` — способ) ④ не округлять частичный ответ владельца до ✅ ⑤ исключение из правила блоков — только на странице паспорта ⑥ **прогонять `npm run prebuild` целиком** — дважды окупилось: гейт типографики и падение `parser-fs` до всех гейтов ⑦ 🪦 **ОТМЕНЁН 2026-08-25 — он и был дефектом.** Стояло: «`check:config-schemas` красен на Windows (CRLF), но `check:blocks-map` бывает и настоящим». Эта запись узаконила недоделку: два сторожа кричали ВСЕГДА, поэтому не значили ничего, а под их постоянной краснотой пряталось настоящее расхождение каталога секций. Причина была одна и лечится один раз — `* text=auto eol=lf` в `.gitattributes` (шаг 21). **Новый закон: гейт, который красен всегда, — это не особенность платформы, а невылеченная болезнь; списывать его на среду запрещено** ⑧ не печатать `git remote -v` сервера ⑨ главная переделывается данными и вызовами, не `rm` ⑩ **не проверять содержимое страницы поиском по телу** — смотреть `<h1>` ⑪ **форма публичной страницы ≠ формы защищённой** ⑫ **удаляя, сперва снять ссылки, потом удалять** — иначе каскад ошибок скрывает настоящие ссылки ⑬ **воспроизводя паттерн, воспроизводить и его регистрацию** в конфигурации сторожа ⑭ **страница может быть опорой гейта** (`/blocks` — образцы всех 31 вида): после удаления гнать весь `prebuild` ⑮ `clinic_id` не переезжает ⑯ текст с экранированием писать файлом, а не через heredoc ⑰ **не принимать отчёт операции за состояние базы** — `report.rows++` считает отправленное, а не легшее; состояние снимать запросом к базе ⑱ **`UNIQUE` с колонкой, где бывает `NULL`, не защищает от повтора** и молчит об этом; лечить в двух местах — схема и код записи ⑲ **сначала диагноз, потом лечение красного гейта**: `npm run build:*` по подсказке гейта — это ловушка, она мусорит отслеживаемым файлом ⑳ **обратные кавычки в комментариях внутри `SCHEMA`** рвут шаблонную строку (оплачено дважды за один день) ㉕ **ограничение частоты принадлежит АДРЕСУ, а не вызывающему** — пауза, оставленная на совести зовущего, будет забыта следующим (491 непрочитанная карточка) ㉖ **число, которое НЕ измерено, не имеет права показываться цифрой** — на экране пишется словом ㉗ **`WITH … SELECT` в слое данных молча возвращает пусто** — писать производную таблицу во `FROM`; `?? 0` превращает «ответа нет» в «ответ ноль» ㉘ **у `attendance` ЧЕТЫРЕ значения** (`1` пришёл, `-1` не пришёл, `0` ждёт отметки, `2` подтверждён) — «всё кроме 1 = прогул» дало долю неявок 79% вместо 4% ㉙ **флаг без потребителя — обещание, которого никто не сдержал** (`excluded` в исходнике не читался нигде) ㉚ **heredoc обрезается на длинном файле** — писать файлы больше ~150 строк частями через `cat >>`

 ㉑ **негативный контроль, совпавший с настоящим ответом, — не контроль**: проверка не измеряет ничего, как бы правдоподобно ни выглядела ㉒ **чужой API молча отдаёт меньше полей, чем просили** — считать ПРИСУТСТВИЕ поля, а не только значение: отсутствующее притворяется пустым ㉓ **копировать у аналога КОНСТРУКЦИЮ, а не вид** — заголовок «как у соседа» мимо примитива ловится гейтом уже после написания ㉔ **план по памяти о прошлой сессии называет не те адреса** — сверять со словарями маршрутов до первой строки |
| **Следующее действие** | 🔴 **Сначала — проверки РУКАМИ РОМЫ, их нельзя сделать за него:** ⓪ ЧАТ (шаг 25, ДВЕ ревизии по его находкам): открыть демо-ветку на `/ru/messages`, написать сообщение, прикрепить картинку, дождаться ответа «клиента» — ни отправка, ни вложение агентом НЕ проверены ни разу, обе двери требуют сессии; ① вписать телефон на `/ru/company`, сохранить и увидеть его в подвале сайта (до этого `tel:` в разметке главной пуст — проверено запросом); ② нажать «Показать демонстрацию» на `/ru/messages`; ③ открыть карточку человека из списка `/ru/patients` — теперь входа два. Затем **шаг 20 — ЯДРО.** Всё, что видно на экранах, готово; работать продукт пока не может. Нужны: ① **два таймера** — мгновенный триггер и расписание (🔒 мера успеха владельца двойная: уведомление получают И те, и другие); ② **отправка ChatPush** (решение владельца: временно); ③ **приём сообщений** — вебхук с дедупом по `chatpush_message_id`, уникальный индекс под него в схеме уже стоит; ④ **тексты сообщений** — `message_goal` правила получает модель. ⚠️ Перед планом прочитать `app/api/chatpush/webhook/route.js` (92 строки) и `lib/ai.js` исходника. Очередь `~/.claude/plans/synchronous-dancing-badger.md` пройдена целиком; номера были сдвинуты на единицу решением Ромы |

### Что открыто после шага 9

- 🔴 **слово на сборку** — шаг 9 закрывается ею: только на живом сайте Рома проверит меню глазами;
- ⚠️ **ожидание против факта:** Рома написал «ящик будет абсолютно чистый после сборки». После двух
  новых правил ящик становится выпадающим меню **для гостя и для `finance`**; архитектору он покажет
  три раздела и двенадцать ссылок — это навигация по утверждённому дереву, вписанная в шаге 7. Если
  пустота ожидалась и для себя, это отмена навигации шага 7, и молча я её не делал;
- 🛑 **виджет `security-orbit`** — из шаблона, в дереве не значится, вызов с главной снят в шаге 7,
  не рисуется нигде. Правило уборки говорит про страницы; виджеты не названы ни в одном списке;
- **выпадающее меню глазами не проверено** — показывается роли без секций, а локально обход выдаёт
  `architect`. Проверяется на сервере гостем или под `finance`;

### Попутные шаги — те, которых не планировали

**13 — согласие и день рождения из CRM.** Родился из дефекта, найденного экраном аудита в 12-4: CRM
молча не отдавала два поля, и продукт считал согласными всех. Вставать в утверждённую очередь ему было
некуда. 🔒 **Решение Ромы 2026-08-25: очередь сдвинута** — «Задачи» стали шагом 14, «Сценарии» 15,
«Услуги» 16, «Переписка» 17, «Аналитика» 18. «Аудит базы» из очереди вычеркнут: сделан в 12-4.

### Открыто после шага 11

- ✅ **закрыто 12-1:** шаг 11 доставлен (`f242bea`), право двери измерено на сервере — гость `401`,
  негативный контроль `/api/health` `200`. ⚠️ Ветка `403` (сессия есть, роль не та) **не измерена**:
  нужна сессия Ромы под `manager`. Это второй такой долг — тот же нужен четвёртому пруфу шага 4;
- 🔴 **ГЛАЗАМИ НЕ ВИДЕЛ НИ ОДИН ЭКРАН** — расширение Chrome отключено (две попытки, дальше не долбил).
  Проверены двери на живых числах, каркас в серверной разметке, право на сервере. НЕ проверено: как
  выглядит список на 1844 строках, ложатся ли колонки карточки на узком экране, читаются ли плитки
  аудита. Нужны глаза Ромы на `/ru/patients`, `/ru/patients/<id>`, `/ru/audit`;
- 🔴 **CRM не отдаёт `sms_not` и `birth_date`** — просим семь полей, приходит пять, молча. Согласие
  выходит всегда «можно писать», цепочка `birthday` не сработает ни разу. Обезврежено надписью «не
  измерено» на экране аудита; лечение — шаг 13;
- **984 визита без человека** — законное состояние, но число должно попасть на экран «Аудит базы»:
  клиника имеет право знать, скольких вернуть невозможно;
- **дубль в CRM** — карточки `356839920` и `356839923` один человек с одним номером; свести на их
  стороне руками;
- **проверки на записанном ответе так и нет** — для шага 12 она станет обязательной: экраны нельзя
  гонять обращениями к живой CRM.

### Что ещё открыто

- 🔴 **перевыпустить токен GitHub** — лежит открытым в `/opt/fractera/app/.git/config`;
- 🔴 **живые креды в переписке, второй раз** (2026-08-24 и 2026-08-25): оба токена YCLIENTS, ключ
  OpenAI, `AUTH_SECRET`, пароль Neon. Перевыпуск — за Ромой;
- **четвёртый пруф шага 4** — «архитектор видит паспорт»: нужна сессия Ромы;
- **развилка `/ru/cabinet` по ролям** и **что оператор редактирует в `/ru/clients`** — при наполнении;
- **префикс таблиц `care_`** — предложение по образцу `tgdesk_`;
- **`use-testing` — четвёртая ложная проверка:** «счётчик отправителя доказывает состояние
  приёмника». Стоила полутора часов и живой таблицы;
- **`use-database` — два закона:** `UNIQUE` с колонкой, где бывает `NULL`, не работает; ограничение
  не догоняет уже созданную таблицу, а `LATE_COLUMNS` добавляет только колонки;
- **девять предложений по навыкам**: `use-roles` §5 · `use-deploy` §5 · `previews.generated` ·
  `CLAUDE.md` про каталог 22 направлений · `use-routes` §2 про глубину · `use-routes` §2 про разную
  форму публичной и защищённой страницы · четвёртый вопрос перед удалением (страница как опора гейта).

### Что осталось в дереве и почему

`/blocks` — **опора гейта, а не витрина**: её `_data/specimen.ts` даёт образец каждому из 31 вида, и без
неё `check:sections` валится десятью нарушениями. Инфраструктура удалению не подлежит.
`/administration/passport` и `/administration/users` — в утверждённом дереве.

### Эталон

Местных образцов почти не осталось. Дальше — публичный стартер
`https://github.com/Fractera/fractera-next-starter`. 🔒 **С ним пока не сверялся ни разу**; скажу вслух
в тот день, когда впервые возьму паттерн оттуда.

### Канал на сервер

`.env.local` из панельной выгрузки, ключ в `.fractera-ssh/`. Собирает **панель**. Команда одна:
`bash scripts/server/deploy.sh <пути>`. `git push` — только токеном из `.env.local`.
🟢 **Пересобрано 2026-08-25 после шага 21:** `===DEPLOY_OK=== 03aee33`, `/api/health` = `03aee33`,
`builtAt` 10:25, pm2 `online`, uptime растёт. Одиннадцать адресов проверены снаружи — 200.

🔒 **ДОСТАВКА АДДИТИВНА — МЕТКА `commit` НЕ ДОКАЗЫВАЕТ СОСТАВ СЛОТА** (оплачено шагом 21). `copy.sh`
перезаписывает названные пути и НИЧЕГО НЕ УДАЛЯЕТ, а метка берётся из `git rev-parse HEAD` на локальной
машине. Сборка честно называет свежий хэш, будучи собранной из старых файлов. Правил мебель (меню,
подвал, макеты, фабрику страниц) — везти дерево целиком, а не тронутые пути.
🔒 **`data` и `storage` в доставку НЕ ВКЛЮЧАТЬ НИКОГДА:** в git там только `.gitkeep`, а `copy.sh`
пакует каталог ЦЕЛИКОМ — локальная пустая база затёрла бы боевую с 1844 карточками.
🔒 **Сборка идёт в режиме `direct`:** `FRACTERA_DEPLOY_SECRET` объявлен, но ПУСТ. Журнал развёртываний
панели держит старую запись, отката панели у наших сборок нет.

---

Разделы выше — и есть форма; пустые не удалять, пустой раздел это тоже ответ.
## Форма записи

Разделы выше — и есть форма; пустые не удалять, пустой раздел это тоже ответ.


| Раздел | Что в нём |
|---|---|
| **Группа** | одной фразой, что строим целиком, и когда начали |
| **Таблица шагов** | номер · что · состояние · **условие закрытия** |
| **Попутные шаги** | те, которых не планировали, — с причиной появления |
| **Условие закрытия группы** | что должно быть верно, чтобы вся группа считалась законченной |
| **Сделано и чем доказано** | по строке на пункт: не «сделал форму», а «форма записана, хэши сошлись» |
| **Решения владельца** | дословно, не пересказ: пересказ теряет то, ради чего решение принято |
| **Чего НЕ делать** | тупики, уже оплаченные в этой группе |
| **Следующее действие** | одна фраза; список — это план, он живёт в файле шага |

---

## Когда этот файл обнуляется

**Агент обнуляет его сам, при закрытии группы, той же операцией.** Содержимое уходит двумя адресами:
итоги шагов — в `completed-steps/`, а картина целого — в **репорт фичи** (`development-docs/reports/`,
категория `feature-`). После этого здесь снова «пусто — работа не начата».

Файл, который не обнуляют, за месяц становится вторым журналом: его чтение само превращается в расход
контекста — ровно в тот, ради экономии которого он заведён.
