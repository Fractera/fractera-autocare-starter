# current-steps.md — состояние проекта между сессиями

## Зачем этот файл существует

**Фича живёт дольше сессии.** От начала разработки до репорта проходит не одна сессия, а иногда
десятки. Каждая новая начинается с пустой головы, и техническое задание шага 6, 8 или 28 ей не
поможет: задание говорит, что задумано, и молчит о том, **что уже сделано и на что смотреть**.

**Поэтому здесь не перечисление шагов, а состояние проекта.** Прочитав только этот файл, агент входит
в разработку сразу — не восстанавливая вчерашний день по коммитам, отчётам и памяти, которой у него
нет.

🔒 **ЭТО НАША ЗАМЕНА `/compact` — И ЗАМЕНА ОБЯЗАТЕЛЬНАЯ** (решение владельца 2026-08-22).
`/compact` сжимает контекст моделью: что именно потеряно, не видит ни агент, ни человек, и пропажа
всплывает через час — забытым решением владельца или законом, который «был в начале сессии».
Здесь состояние записано **буквами, в файл**: его можно прочитать, проверить и поправить, и оно
переживает не только сброс, но и смену модели.

**Один файл на весь проект.** Переписывается ПОВЕРХ, а не дописывается. Всё, что уже стало итогом,
отсюда уходит в `completed-steps/`.

**Имя во множественном числе намеренно:** работа почти никогда не один шаг. Это группа связанных шагов
и подшагов, и половина смысла файла — держать их связи и условия закрытия, которых нет больше нигде.

**Читается ПЕРВЫМ в начале КАЖДОЙ сессии**, раньше плана шага и раньше кода.

---

## Сейчас в работе

**Группа «Переезд Автозаботы на Fractera».** Начата 2026-08-24. Шаги 1–7 закрыты.
🟢 **Утверждённое дерево существует скелетом:** 14 адресов, 14 различных заголовков, навигация ходит.
⛔ **Наполнения нет ни на одном** — ни данных, ни компонентов, ни логики.

| Раздел | Состояние |
|---|---|
| **Группа** | перенести продукт «Автозабота» (исходник Филиппа `carecrm`) на архитектуру Fractera. Начата 2026-08-24 |
| **Шаги** | **1** инвентаризация · **2** паспорт · **3** страница паспорта · **4** первое обновление сервера · **5** тип и роли · **6** проектирование маршрутов · **7** скелет дерева — **все ЗАКРЫТЫ**. **8 — не заведён** |
| **Условие закрытия группы** | таймерные цепочки работают на своём сервере со своей базой. 🔒 Мера успеха двойная: уведомление получают И те, кто попал в мгновенный триггер, И те, кто попал в рассылку по расписанию |
| **Сделано и чем доказано** | ① карта исходника ② паспорт ③ CRM = **YCLIENTS**, четыре следа ④ страница паспорта: чужая роль `403 {"requires":["architect"]}` ⑤ **сервер обновлён:** `commit` = `3b24467`, `status.sh` = 0, гость получает `401` и объясняющее окно ⑥ тип `scheduler` и четыре роли ⑦ архитектура согласована (`PASSPORT.md` §11) ⑧ **скелет построен:** 14 адресов, 14 РАЗЛИЧНЫХ `<h1>` при негативном контроле 404; старое дерево цело (6 прежних адресов отвечают 200); 19 гейтов зелёные |
| **Решения владельца** | 🔒 тип **`scheduler`** · роли **`user`, `vip_user`, `manager`, `admin`** · **главная: бейджи, название компании, одна кнопка «Войти»** · вход **только по email**, связывание с телефоном вне миграции · после входа развилка **по роли** · кабинет — минимальная заглушка · **продукт в `PRODUCTS-CONFIG` не заводим**, работаем классически · права админа — **вариант B**, `lib/roles.ts` не трогаем · отправка **ChatPush, временно** · мера успеха — оба пути · цепочку будят схемы автоматизаций со скриптом от ИИ · одно приложение — одно учреждение · регулятора временно не оптимизируем · сначала создать, потом удалять. Все дословно в `PASSPORT.md` |
| **Чего НЕ делать** | ① не выводить задание из наличия папки в шаблоне ② не переносить факт об исходнике как факт о проекте — и обратно тоже ③ не измерять право на машине с обходом (`NODE_ENV=development` → всем `architect`; чужая роль — заголовком `x-agent-identity`) ④ не округлять частичный ответ владельца до ✅ ⑤ исключение из правила блоков — только на странице паспорта ⑥ **прогонять `npm run prebuild` целиком** ⑦ **`check:config-schemas` красен на Windows** — это CRLF, на сервере зелено; но **`check:blocks-map` бывает и настоящим**, сверять со снятой ЭКРАНИРОВАННОЙ последовательностью, а не байтом ⑧ не печатать `git remote -v` сервера — там живой токен ⑨ **главная переделывается данными и вызовами, не `rm`** ⑩ **не проверять содержимое страницы поиском по телу** — ящик навигации печатает заголовки всех страниц на каждой; смотреть `<h1>` ⑪ **форма публичной страницы ≠ формы защищённой**: публичная собирается `createContentPage` и обязана иметь `index.md`-двойника ⑫ `clinic_id` не переезжает ⑬ текст с экранированием писать файлом, а не через heredoc |
| **Следующее действие** | Рома называет шаг 8. Дерево готово принимать наполнение |

### Что открыто

- 🔴 **перевыпустить токен GitHub** — лежит открытым в `/opt/fractera/app/.git/config` и попал в переписку;
- **четвёртый пруф шага 4** — «архитектор видит паспорт»: нужна сессия Ромы;
- **развилка `/ru/cabinet` по ролям** и **что именно оператор редактирует в `/ru/clients`** — решается
  при наполнении этих страниц;
- **префикс таблиц `care_`** — предложение по образцу `tgdesk_`, ждёт слова;
- **шесть предложений по навыкам** ждут слова: `use-roles` §5 · `use-deploy` §5 · запись про
  `previews.generated` · строка в `CLAUDE.md` про каталог 22 направлений · `use-routes` §2 про глубину
  в защищённом слое · `use-routes` §2 про разную форму публичной и защищённой страницы.

### Как теперь устроен канал на сервер

`.env.local` из панельной выгрузки, ключ в `.fractera-ssh/fractera-agent-key`. Собирает **панель**
(`FRACTERA_DEPLOY_SECRET` есть). Команда одна: `bash scripts/server/deploy.sh <пути>`.
🔒 **`git push` идёт только токеном из `.env.local`** — окружение аутентифицируется как `bolshiyanov`,
прав у него нет (403). 🔒 **Слот подтягивает GitHub сам.**
⚠️ **Скелет на сервер НЕ доставлен** — сборка только по слову владельца.

### Открытые вопросы к ядру — к своему шагу

Полностью — `PASSPORT.md` §9. Ближайшие: где живёт фоновый процесс автоматизаций и чем запускается на
Ubuntu · что делать со старым скриптом при повторной генерации ИИ · как хранится и валидируется «схема
автоматизации» · словарь предметной области.

---

Полностью — `PASSPORT.md` §9. Ближайшие: где живёт фоновый процесс автоматизаций и чем запускается на
Ubuntu · что делать со старым скриптом при повторной генерации ИИ · как хранится и валидируется «схема
автоматизации» · словарь предметной области · заводится ли продукт в панели и под каким `id`.

---

## Форма записи

Разделы выше — и есть форма; пустые не удалять, пустой раздел это тоже ответ.

| Раздел | Что в нём |
|---|---|
| **Группа** | одной фразой, что строим целиком, и когда начали |
| **Таблица шагов** | номер · что · состояние · **условие закрытия** |
| **Попутные шаги** | те, которых не планировали, — с причиной появления |
| **Условие закрытия группы** | что должно быть верно, чтобы вся группа считалась законченной |
| **Сделано и чем доказано** | по строке на пункт: не «сделал форму», а «форма записана, хэши сошлись» |
| **Решения владельца** | дословно, не пересказ: пересказ теряет то, ради чего решение принято |
| **Чего НЕ делать** | тупики, уже оплаченные в этой группе |
| **Следующее действие** | одна фраза; список — это план, он живёт в файле шага |

---

## Когда этот файл обнуляется

**Агент обнуляет его сам, при закрытии группы, той же операцией.** Содержимое уходит двумя адресами:
итоги шагов — в `completed-steps/`, а картина целого — в **репорт фичи** (`development-docs/reports/`,
категория `feature-`). После этого здесь снова «пусто — работа не начата».

Файл, который не обнуляют, за месяц становится вторым журналом: его чтение само превращается в расход
контекста — ровно в тот, ради экономии которого он заведён.
