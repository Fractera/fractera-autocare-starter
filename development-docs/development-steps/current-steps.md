# current-steps.md — состояние проекта между сессиями

## Зачем этот файл существует

**Фича живёт дольше сессии.** От начала разработки до репорта проходит не одна сессия, а иногда
десятки. Каждая новая начинается с пустой головы, и техническое задание шага 6, 8 или 28 ей не
поможет: задание говорит, что задумано, и молчит о том, **что уже сделано и на что смотреть**.

**Поэтому здесь не перечисление шагов, а состояние проекта.** Прочитав только этот файл, агент входит
в разработку сразу — не восстанавливая вчерашний день по коммитам, отчётам и памяти, которой у него
нет.

🔒 **ЭТО НАША ЗАМЕНА `/compact` — И ЗАМЕНА ОБЯЗАТЕЛЬНАЯ** (решение владельца 2026-08-22).
`/compact` сжимает контекст моделью: что именно потеряно, не видит ни агент, ни человек, и пропажа
всплывает через час — забытым решением владельца или законом, который «был в начале сессии».
Здесь состояние записано **буквами, в файл**: его можно прочитать, проверить и поправить, и оно
переживает не только сброс, но и смену модели.

**Один файл на весь проект.** Переписывается ПОВЕРХ, а не дописывается. Всё, что уже стало итогом,
отсюда уходит в `completed-steps/`.

**Имя во множественном числе намеренно:** работа почти никогда не один шаг. Это группа связанных шагов
и подшагов, и половина смысла файла — держать их связи и условия закрытия, которых нет больше нигде.

**Читается ПЕРВЫМ в начале КАЖДОЙ сессии**, раньше плана шага и раньше кода.

---

## Сейчас в работе

**Группа «Переезд Автозаботы на Fractera».** Начата 2026-08-24. Шаги 1, 2 и 3 закрыты.
🟢 **Светофор запуска чист — разработка ядра разрешена.** Шаг 3 был первым с кодом.

| Раздел | Состояние |
|---|---|
| **Группа** | перенести продукт «Автозабота» (исходник Филиппа `carecrm`) на архитектуру Fractera. Начата 2026-08-24 |
| **Шаги** | **1 — инвентаризация исходника · ЗАКРЫТ** (карта в `reports/migration-…md`; два вывода отменены, надгробие в `1-main.md`). **2 — паспорт проекта · ЗАКРЫТ** (`development-docs/PASSPORT.md`). **3 — ответы Ромы + защищённая страница паспорта · ЗАКРЫТ** (подшаги 3-1, 3-2). **4 — не заведён** |
| **Условие закрытия группы** | таймерные цепочки работают на своём сервере со своей базой. 🔒 Мера успеха двойная (решение Ромы): уведомление получают И те, кто попал в мгновенный триггер, И те, кто попал в рассылку по расписанию. Один путь без второго — половина продукта |
| **Сделано и чем доказано** | ① исходник прочитан целиком, карта построена — грепом на планировщик (0 при негативном контроле 4) и README исходника. ② паспорт заведён — состоянием машины (`.env.local`: доменный режим, дублируются ДВА ключа) и состоянием документов. ③ CRM доказана — **YCLIENTS**, четыре следа в исходнике (домен в коде, схема базы, переменные окружения, 12 файлов без конкурента). ④ страница паспорта построена — чужая роль получает `403 {"requires":["architect"]}` при негативном контроле 200 на незащищённой двери; в оболочке страницы **0** признаков паспорта при негативном контроле 1 на признаках каркаса |
| **Решения владельца** | 🔒 **закон старта:** «Разработка не начинается, пока из паспорта не понятен минимально достаточный набор данных о проекте». 🔒 **наличие ≠ задание:** «Выбор инструмента есть решение архитектора, а не вывод из наличия папки в шаблоне». 🔒 **отправка:** ChatPush по API, **временно**; служба Telegram шаблона кандидатом никогда не была. 🔒 **мера успеха:** «работают ОБА пути… один путь без второго — половина продукта». 🔒 **чем будится цепочка:** «схемы готовых автоматизаций, которые настраивает администратор», скрипт под каждую **создаётся ИИ** и работает в фоне — «возможно именно потому, что сервер свой (Ubuntu), а не Vercel». 🔒 **арендаторы:** «Одно приложение — всегда одно учреждение». 🔒 **регулятор:** «в текущей версии работу с регулятором мы не оптимизируем, осознанно и на свой страх и риск» — **временно**. 🔒 **адрес:** значение вычисляется с точки зрения ТОЙ машины. 🔒 **порядок:** сначала создаются нужные структуры, потом удаляются предустановленные. Все — дословно в `PASSPORT.md`, 2026-08-24 |
| **Чего НЕ делать** | ① не выводить задание из наличия папки в шаблоне (оплачено дважды в шаге 1). ② не переносить факт об ИСХОДНИКЕ как факт о ПРОЕКТЕ по правдоподобию. ③ **не измерять право на машине с обходом**: `shouldBypassAuth()` возвращает `true` безусловно при `NODE_ENV=development` — дев-сервер выдаёт всем `architect`. Чужая роль проверяется заголовком `x-agent-identity` (ветка стоит ДО обхода). ④ не округлять частичный ответ владельца до ✅ — разделять на две строки. ⑤ не переносить исключение из правила блоков на другие страницы: оно действует только на паспорт. ⑥ не править `.env.local` — файл трогает Рома. ⑦ не создавать `middleware.js`. ⑧ `clinic_id` в результат не переезжает |
| **Следующее действие** | 🛑 **Шаг 4 остановлен на предусловии** — см. блок ниже. Нужно слово Ромы: коммитить и отправлять в GitHub |

### 🛑 Шаг 4 — остановлен на предусловии, 2026-08-24

Шаг 4 (первое обновление сервера) начинается с проверки «репозиторий отправлен в GitHub». **Не
отправлен, и расхождение больше, чем описано в плане шага.**

| Что | Состояние |
|---|---|
| локальный `HEAD` | `462aaac` |
| `origin/main` в GitHub | `46508d5` |
| разница | **8 коммитов**, не 6: план писался до двух последних |
| **чего нет ни в одном коммите** | **весь код шага 3** — `app/api/passport/`, `app/[lang]/(protectedLayer)/(admin)/administration/passport/`, `lib/i18n/passport-link.i18n.ts`, правки подвала, `check-i18n.mjs`, `package.json`; плюс итоги `3-1/3-2/3-main` и правки паспорта из шага 3 |

🔒 **Предпосылка плана «в этих коммитах весь паспорт со страницей» — неверна.** Все 8 коммитов — Ромины,
в них планы шагов и документы. Проверено: `git cat-file -e HEAD:app/api/passport/route.ts` →
`exists on disk, but not in HEAD`; файлов `administration/passport` в `HEAD` — **ноль**.

**Почему код не в коммитах:** агент не коммитит без слова владельца, и слова не было. Это не дефект
работы, а недостающее звено цикла `pull → edit → push → build`.

**Что разблокирует, одной операцией:** закоммитить рабочее дерево и отправить (`git push origin main`)
— 8 Роминых коммитов уедут вместе с ним. После этого шаг 4 идёт как написан: `deploy.sh`, канал
панели (`FRACTERA_DEPLOY_SECRET` в окружении есть, `FRACTERA_SSH_*` нет и не нужны), четыре
доказательства.

**Чего НЕ делал:** не коммитил, не отправлял, `deploy.sh` не запускал. Отправка — действие наружу и
решение владельца.

### Что нужно от Ромы, но работу не держит

- **развернуть шаг 3 на боевом сервере** — два ответа приёмки локально недоказуемы: гость → 401 и
  архитектор → видит паспорт. Проверяются одной командой каждая, когда он скажет;
- **два предложения по навыкам** ждут его слова (см. `3-main.md`): строка в `use-roles` §5 про
  `x-agent-identity` как способ проверить право на машине с обходом; запись в `ANTI-PATTERNS.md` про
  `previews.generated` в свежем клоне;
- `.env.local`: дублируются **два** ключа — `REMOTE_DATA_URL` (значения разные, вредит) и
  `DATA_SECRET` (значения совпадают, безвреден). Верное значение — `https://data.aifa.dev`.

### Открытые вопросы к ядру — к своему шагу, не к старту

Полностью — `PASSPORT.md` §9. Ближайшие: где физически живёт фоновый процесс автоматизаций и чем
запускается на Ubuntu · что делать со старым скриптом при повторной генерации ИИ · как хранится и
валидируется «схема автоматизации» · словарь предметной области («пациент» или нейтральное) ·
заводится ли продукт в панели и под каким `id` — на нём висят имена таблиц.

---

## Форма записи

Разделы выше — и есть форма; пустые не удалять, пустой раздел это тоже ответ.

| Раздел | Что в нём |
|---|---|
| **Группа** | одной фразой, что строим целиком, и когда начали |
| **Таблица шагов** | номер · что · состояние · **условие закрытия** |
| **Попутные шаги** | те, которых не планировали, — с причиной появления |
| **Условие закрытия группы** | что должно быть верно, чтобы вся группа считалась законченной |
| **Сделано и чем доказано** | по строке на пункт: не «сделал форму», а «форма записана, хэши сошлись» |
| **Решения владельца** | дословно, не пересказ: пересказ теряет то, ради чего решение принято |
| **Чего НЕ делать** | тупики, уже оплаченные в этой группе |
| **Следующее действие** | одна фраза; список — это план, он живёт в файле шага |

---

## Когда этот файл обнуляется

**Агент обнуляет его сам, при закрытии группы, той же операцией.** Содержимое уходит двумя адресами:
итоги шагов — в `completed-steps/`, а картина целого — в **репорт фичи** (`development-docs/reports/`,
категория `feature-`). После этого здесь снова «пусто — работа не начата».

Файл, который не обнуляют, за месяц становится вторым журналом: его чтение само превращается в расход
контекста — ровно в тот, ради экономии которого он заведён.
