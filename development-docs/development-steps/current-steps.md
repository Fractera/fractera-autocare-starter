# current-steps.md — состояние проекта между сессиями

## Зачем этот файл существует

**Фича живёт дольше сессии.** От начала разработки до репорта проходит не одна сессия, а иногда
десятки. Каждая новая начинается с пустой головы, и техническое задание шага 6, 8 или 28 ей не
поможет: задание говорит, что задумано, и молчит о том, **что уже сделано и на что смотреть**.

**Поэтому здесь не перечисление шагов, а состояние проекта.** Прочитав только этот файл, агент входит
в разработку сразу — не восстанавливая вчерашний день по коммитам, отчётам и памяти, которой у него
нет.

🔒 **ЭТО НАША ЗАМЕНА `/compact` — И ЗАМЕНА ОБЯЗАТЕЛЬНАЯ** (решение владельца 2026-08-22).
`/compact` сжимает контекст моделью: что именно потеряно, не видит ни агент, ни человек, и пропажа
всплывает через час — забытым решением владельца или законом, который «был в начале сессии».
Здесь состояние записано **буквами, в файл**: его можно прочитать, проверить и поправить, и оно
переживает не только сброс, но и смену модели.

**Один файл на весь проект.** Переписывается ПОВЕРХ, а не дописывается. Всё, что уже стало итогом,
отсюда уходит в `completed-steps/`.

**Имя во множественном числе намеренно:** работа почти никогда не один шаг. Это группа связанных шагов
и подшагов, и половина смысла файла — держать их связи и условия закрытия, которых нет больше нигде.

**Читается ПЕРВЫМ в начале КАЖДОЙ сессии**, раньше плана шага и раньше кода.

---

## Сейчас в работе

**Группа «Переезд Автозаботы на Fractera».** Начата 2026-08-24. Шаги 1–4 закрыты.
🟢 **Работа впервые вышла на боевой сервер.** `https://aifa.dev/ru/administration/passport` живёт.

| Раздел | Состояние |
|---|---|
| **Группа** | перенести продукт «Автозабота» (исходник Филиппа `carecrm`) на архитектуру Fractera. Начата 2026-08-24 |
| **Шаги** | **1 — инвентаризация · ЗАКРЫТ** (карта в `reports/migration-…md`). **2 — паспорт проекта · ЗАКРЫТ** (`PASSPORT.md`). **3 — ответы Ромы + защищённая страница · ЗАКРЫТ**. **4 — первое обновление сервера · ЗАКРЫТ** (3 пруфа из 4). **5 — не заведён** |
| **Условие закрытия группы** | таймерные цепочки работают на своём сервере со своей базой. 🔒 Мера успеха двойная: уведомление получают И те, кто попал в мгновенный триггер, И те, кто попал в рассылку по расписанию. Один путь без второго — половина продукта |
| **Сделано и чем доказано** | ① карта исходника — грепом на планировщик (0 при негативном контроле 4). ② паспорт заведён — состоянием машины и документов. ③ CRM = **YCLIENTS**, четыре следа в исходнике. ④ страница паспорта построена — чужая роль `403 {"requires":["architect"]}`. ⑤ **сервер обновлён:** `commit` в `/api/health` = `3b24467` (было `null`), `status.sh` = 0, порт за pm2, uptime 8760→12299; гость получает 200 + `401` и окно `AccessGate` с путём входа, содержимого паспорта в его разметке — 0 |
| **Решения владельца** | 🔒 **закон старта** · 🔒 **наличие ≠ задание** · 🔒 **отправка — ChatPush, временно** · 🔒 **мера успеха — оба пути** · 🔒 **цепочку будят схемы автоматизаций, скрипт под каждую создаёт ИИ, работает в фоне** · 🔒 **одно приложение — одно учреждение** · 🔒 **регулятора временно не оптимизируем** · 🔒 **адрес вычисляется с точки зрения ТОЙ машины** · 🔒 **порядок: сначала создать, потом удалять**. Все дословно в `PASSPORT.md` |
| **Чего НЕ делать** | ① не выводить задание из наличия папки в шаблоне. ② не переносить факт об исходнике как факт о проекте. ③ не измерять право на машине с обходом (`NODE_ENV=development` → всем `architect`; чужая роль проверяется заголовком `x-agent-identity`). ④ не округлять частичный ответ владельца до ✅. ⑤ исключение из правила блоков — только на странице паспорта. ⑥ **не прогонять выбранные гейты вместо `npm run prebuild` целиком** — список ведёт `package.json`. ⑦ **не «чинить» `check:config-schemas` и `check:blocks-map` на Windows** — это CRLF, на сервере зелено; сверять содержимое без переводов строк и откатывать. ⑧ **не печатать `git remote -v` сервера** — в адресе встроен живой токен. ⑨ не создавать `middleware.js`. ⑩ `clinic_id` не переезжает |
| **Следующее действие** | Рома входит архитектором и подтверждает четвёртый пруф; называет шаг 5 |

### Что открыто

- 🔴 **перевыпустить токен GitHub** — лежит открытым в `/opt/fractera/app/.git/config` и попал в
  переписку; после перевыпуска панель перезапишет его сама;
- **четвёртый пруф шага 4** — «архитектор видит паспорт»: нужна сессия Ромы, вкладка оставлена
  открытой на нужной странице;
- **три предложения по навыкам** ждут слова: `use-roles` §5 (проверка права на машине с обходом через
  `x-agent-identity`), `use-deploy` §5 (пятая ловушка: прогонять весь `prebuild`), и запись про
  `previews.generated` в свежем клоне;
- ✅ **закрыто:** дубли в выгрузке окружения. В новой выгрузке их нет, `REMOTE_DATA_URL` один и верный.

### Как теперь устроен канал на сервер

Транспорт работает: `.env.local` из панельной выгрузки, ключ развёрнут в
`.fractera-ssh/fractera-agent-key` (папка под `.gitignore`). Сборку запускает **панель**
(`FRACTERA_DEPLOY_SECRET` есть) — её очередь, журнал и откат. Команда одна:
`bash scripts/server/deploy.sh <пути>`.

🔒 **`git push` идёт только токеном из `.env.local`:** окружение аутентифицируется как `bolshiyanov`,
у которого нет прав на репозиторий (403).

🔒 **Слот подтягивает GitHub сам** — на момент шага 4 он уже стоял на моём коммите, не собранный.
Значит обычный цикл: `push` → `deploy.sh` без путей (пересборка). Пути нужны, чтобы увидеть правку
сразу, но переживает развёртывание только то, что в репозитории.

### Открытые вопросы к ядру — к своему шагу, не к старту

Полностью — `PASSPORT.md` §9. Ближайшие: где физически живёт фоновый процесс автоматизаций и чем
запускается на Ubuntu · что делать со старым скриптом при повторной генерации ИИ · как хранится и
валидируется «схема автоматизации» · словарь предметной области · заводится ли продукт в панели и под
каким `id`.

---

## Форма записи

Разделы выше — и есть форма; пустые не удалять, пустой раздел это тоже ответ.

| Раздел | Что в нём |
|---|---|
| **Группа** | одной фразой, что строим целиком, и когда начали |
| **Таблица шагов** | номер · что · состояние · **условие закрытия** |
| **Попутные шаги** | те, которых не планировали, — с причиной появления |
| **Условие закрытия группы** | что должно быть верно, чтобы вся группа считалась законченной |
| **Сделано и чем доказано** | по строке на пункт: не «сделал форму», а «форма записана, хэши сошлись» |
| **Решения владельца** | дословно, не пересказ: пересказ теряет то, ради чего решение принято |
| **Чего НЕ делать** | тупики, уже оплаченные в этой группе |
| **Следующее действие** | одна фраза; список — это план, он живёт в файле шага |

---

## Когда этот файл обнуляется

**Агент обнуляет его сам, при закрытии группы, той же операцией.** Содержимое уходит двумя адресами:
итоги шагов — в `completed-steps/`, а картина целого — в **репорт фичи** (`development-docs/reports/`,
категория `feature-`). После этого здесь снова «пусто — работа не начата».

Файл, который не обнуляют, за месяц становится вторым журналом: его чтение само превращается в расход
контекста — ровно в тот, ради экономии которого он заведён.
