# current-steps.md — состояние проекта между сессиями

## Зачем этот файл существует

**Фича живёт дольше сессии.** От начала разработки до репорта проходит не одна сессия, а иногда
десятки. Каждая новая начинается с пустой головы, и техническое задание шага 6, 8 или 28 ей не
поможет: задание говорит, что задумано, и молчит о том, **что уже сделано и на что смотреть**.

**Поэтому здесь не перечисление шагов, а состояние проекта.** Прочитав только этот файл, агент входит
в разработку сразу — не восстанавливая вчерашний день по коммитам, отчётам и памяти, которой у него
нет.

🔒 **ЭТО НАША ЗАМЕНА `/compact` — И ЗАМЕНА ОБЯЗАТЕЛЬНАЯ** (решение владельца 2026-08-22).
`/compact` сжимает контекст моделью: что именно потеряно, не видит ни агент, ни человек, и пропажа
всплывает через час — забытым решением владельца или законом, который «был в начале сессии».
Здесь состояние записано **буквами, в файл**: его можно прочитать, проверить и поправить, и оно
переживает не только сброс, но и смену модели.

**Один файл на весь проект.** Переписывается ПОВЕРХ, а не дописывается. Всё, что уже стало итогом,
отсюда уходит в `completed-steps/`.

**Имя во множественном числе намеренно:** работа почти никогда не один шаг. Это группа связанных шагов
и подшагов, и половина смысла файла — держать их связи и условия закрытия, которых нет больше нигде.

**Читается ПЕРВЫМ в начале КАЖДОЙ сессии**, раньше плана шага и раньше кода.

---

## Сейчас в работе

**Группа «Переезд Автозаботы на Fractera».** Начата 2026-08-24. Шаги 1 и 2 закрыты.
🔒 **Код не начинается**: в паспорте шесть незакрытых строк минимально достаточного набора.

| Раздел | Состояние |
|---|---|
| **Группа** | перенести продукт «Автозабота» (исходник Филиппа `carecrm`) на архитектуру Fractera. Начата 2026-08-24 |
| **Шаги** | **1 — инвентаризация исходника · ЗАКРЫТ** (подшаги 1-1, 1-2; карта — `reports/migration-source-inventory-…md`; два вывода отменены поправкой Ромы, надгробие в `1-main.md`). **2 — паспорт проекта · ЗАКРЫТ** (подшаги 2-1, 2-2; `development-docs/PASSPORT.md`). **3 — не заведён**, и завестись не может до ответов ниже |
| **Условие закрытия группы** | таймерные цепочки работают на своём сервере со своей базой: событие срабатывает по таймеру без человека, сообщение уходит через ChatPush, результат записывается. Не «интерфейс перенесён» |
| **Сделано и чем доказано** | ① исходник прочитан целиком (54 файла, 5444 строки), карта построена — доказано грепом на планировщик (0 строк при негативном контроле в 4) и двумя абзацами README исходника. ② паспорт заведён и наполнен — доказано состоянием машины (`.env.local`: доменный режим ✅, и дублируются ДВА ключа, не один) и состоянием документов (отменённых формулировок в действующем тексте — ноль, при негативном контроле в 4 живых попадания) |
| **Решения владельца** | 🔒 **закон старта:** «Разработка не начинается, пока из паспорта не понятен минимально достаточный набор данных о проекте. Это условие запуска, а не пожелание: не хватает понимания — задаёшь вопросы Роме и ждёшь, а не строишь по догадке». 🔒 **наличие ≠ задание:** «Их наличие — задел под возможные будущие продуктовые фичи, а не задание… Выбор инструмента есть решение архитектора, а не вывод из наличия папки в шаблоне». 🔒 **отправка:** «Отправкой сообщений в этом проекте занимается сторонний сервис ChatPush, подключаемый по API. Служба Telegram, найденная в шаблоне, к задаче отношения не имеет и кандидатом никогда не была». 🔒 **адрес:** «Значение, передаваемое другой машине, вычисляется с точки зрения ТОЙ машины и никогда не копируется из конфигурации сервера». 🔒 **порядок:** «Сначала создаются нужные маршруты и структуры в них, и только потом удаляются предустановленные по умолчанию. Не наоборот». Все — дословно, 2026-08-24 |
| **Чего НЕ делать** | ① не выводить задание из наличия папки в шаблоне — это оплачено дважды в шаге 1 (служба каналов как «кандидат на отправку», `tick` как «готовая стрелка»). ② не переносить факт об ИСХОДНИКЕ в паспорт как факт о ПРОЕКТЕ по правдоподобию (едва не случилось с YCLIENTS). ③ не начинать код, пока в `PASSPORT.md` §2 есть ⛔. ④ не править `.env.local` — файл трогает Рома. ⑤ не заводить свою папку учёта — `check:steps` валит сборку. ⑥ не создавать `middleware.js` — гейт в `proxy.ts`. ⑦ отменяя утверждение, искать его следы грепом по формулировке, а не памятью |
| **Следующее действие** | Рома отвечает на шесть блокирующих вопросов паспорта (§9, строки 1–6) — после этого заводится шаг 3 |

### Чем блокировано — шесть строк паспорта

Полностью — `PASSPORT.md` §2 и §9. Коротко:

1. **мера успеха** — по какой цифре понимают, что продукт работает;
2. **ChatPush**: чей аккаунт, какие каналы, рассылка массовая или по одному, ограничения частоты;
3. **поставщик данных**: YCLIENTS или другая CRM — Ромой поимённо не назван;
4. **чем будится цепочка** — механизм таймера назначает архитектор;
5. **`clinic_id`** — одна клиника или несколько;
6. **персональные данные** — что уходит наружу и уходит ли вообще.

Не блокируют, но ждут: заводить ли продукт в панели и под каким `id` (сегодня `PRODUCTS-CONFIG` пуст,
а на `id` висят имена таблиц и пути логики) · нужна ли публичная часть · нужен ли AI · когда данные
LS centr · переиздавать ли креды, попавшие в чат 2026-08-24.

### Что лежит готовым к шагу 3

Предложение по скелету из закрытого шага 1 (адреса под §4 карты, расщепление `patients` на
`<id>_people` / `<id>_cases`, роли `operator → manager`, вход через службу `:3001`) — **не задание, а
заготовка**. Часть его зависит от ответов 5 и 6, поэтому целиком он не стартует даже как «безопасный».

---

## Форма записи

Разделы выше — и есть форма; пустые не удалять, пустой раздел это тоже ответ.

| Раздел | Что в нём |
|---|---|
| **Группа** | одной фразой, что строим целиком, и когда начали |
| **Таблица шагов** | номер · что · состояние · **условие закрытия** |
| **Попутные шаги** | те, которых не планировали, — с причиной появления |
| **Условие закрытия группы** | что должно быть верно, чтобы вся группа считалась законченной |
| **Сделано и чем доказано** | по строке на пункт: не «сделал форму», а «форма записана, хэши сошлись» |
| **Решения владельца** | дословно, не пересказ: пересказ теряет то, ради чего решение принято |
| **Чего НЕ делать** | тупики, уже оплаченные в этой группе |
| **Следующее действие** | одна фраза; список — это план, он живёт в файле шага |

---

## Когда этот файл обнуляется

**Агент обнуляет его сам, при закрытии группы, той же операцией.** Содержимое уходит двумя адресами:
итоги шагов — в `completed-steps/`, а картина целого — в **репорт фичи** (`development-docs/reports/`,
категория `feature-`). После этого здесь снова «пусто — работа не начата».

Файл, который не обнуляют, за месяц становится вторым журналом: его чтение само превращается в расход
контекста — ровно в тот, ради экономии которого он заведён.
