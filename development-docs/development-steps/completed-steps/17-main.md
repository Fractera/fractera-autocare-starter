# 17 — «Переписка»: ветки разговоров по номеру телефона

**Закрыт 2026-08-25.** Экран готов и пуст по данным: канал связи не подключён.

## Что сделано

`/ru/messages` — список веток и разговор целиком. Дверь `GET /api/care/messages`: без `?phone=`
отдаёт ветки, с ним — сообщения. Разделять на два маршрута незачем: это один предмет с общим правилом
доступа.

## 🔒 Ветка привязана к ТЕЛЕФОНУ, а не к человеку

Закон схемы шага 10, перенесённый из исходника. Входящее записывается раньше, чем номер сопоставлен с
карточкой, и разговоры с незнакомых номеров тоже надо видеть: **это живые обращения, которые кто-то
должен разобрать руками.**

✗ Ключевание по `person_id` потеряло бы ровно те ветки, ради которых экран и нужен: неизвестный номер
остался бы невидимым, и никто бы не узнал, что человек писал. Экран помечает такие ветки словом
«номера нет в базе», а не прячет.

## Решение: один запрос вместо трёх склеек

✗ В исходнике список веток — **три отдельные выборки, склеенные по телефону в JavaScript**, и причина
названа прямо в его коде: «Postgres строг к GROUP BY». У нас слой данных на SQLite, коррелированный
подзапрос законен, и три обращения по сети вместо одного были бы платой за чужое ограничение.

## Ещё одно решение

**Авторство модели названо в каждом сообщении.** Оператор обязан знать, писал он это сам или ответила
модель: от этого зависит, можно ли верить обещаниям в тексте.

## Доказательство

    GET /api/care/messages → {"ok":true,"threads":[],"summary":{"threads":0,"messages":0,
                              "unknownNumbers":0,"aiReplies":0}}
    /ru/messages → HTTP 200

Пусто честно: канал связи не подключён, и экран говорит это словами — «сообщения приходят через канал
связи, и он пока не подключён», а не показывает пустую таблицу.

Гейты: 17 из 17 зелёные.

## Что осталось долгом

- **вебхук приёма сообщений** — в исходнике есть (`chatpush/webhook`, 92 строки, дедуп по
  `chatpush_message_id`; уникальный индекс под него в схеме уже стоит). Перенос — часть ядра (шаг 19+),
  вместе с отправкой;
- **ответ из интерфейса** — писать человеку можно будет, когда появится канал.
