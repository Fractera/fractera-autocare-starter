# PASSPORT.md — паспорт проекта «Автозабота»

> **Заведён 2026-08-24, шаг 2.** Отвечает на один вопрос, на который не отвечает ни один другой файл:
> **что это за проект и по каким правилам он устроен.** План шага говорит о задуманном, итог — о
> сделанном, `current-steps.md` — о состоянии работы; знание о самом проекте до этого файла жило
> только в переписке с Ромой.
>
> 🔒 **Паспорт эволюционирует.** Каждое новое решение и каждая проверенная гипотеза дописываются сюда
> **в тот же заход**, а не «потом». Решения Ромы — дословно: пересказ теряет то, ради чего решение
> принято.
>
> **Пометки достоверности, обязательны у каждого утверждения:**
> ✅ проверено машиной в этом репозитории (рядом — чем) · 📄 взято из документа проекта (рядом — из
> какого) · 🗣 принято со слов (рядом — чьих, и это не факт, пока не проверено) · ⛔ неизвестно.

---

## 1. 🔒 Закон старта

**Решение Ромы, 2026-08-24, дословно:**

> **«Разработка не начинается, пока из паспорта не понятен минимально достаточный набор данных о
> проекте.** Это условие запуска, а не пожелание: не хватает понимания — задаёшь вопросы Роме и ждёшь,
> а не строишь по догадке.»

> «Пользовательских кейсов может не быть вовсе — владелец вправе их не заводить. Тогда паспорт
> остаётся единственным источником понимания, и его неполнота означает не «меньше контекста», а
> **работу вслепую**.»

✗ **Дефект, купивший этот закон — из этого же проекта, 2026-08-24.** В шаге 1 агент разобрал службу
Telegram шаблона как кандидата на отправку сообщений и построил вокруг неё «главный вопрос переезда».
Рассуждение было корректным — не хватало **входных данных, которых на старте не существовало нигде**:
отправкой занимается внешний ChatPush, и Рома сказал это часом позже. Не ошибка рассуждения, а
отсутствие паспорта.

🔒 **Второй закон, выведенный оттуда же (Рома, 2026-08-24), дословно:**

> «Архитектура Fractera поставляет множество инструментов по умолчанию. **Их наличие — задел под
> возможные будущие продуктовые фичи, а не задание.** Увидел инструмент и не получил задания от
> архитектора — применять его по своему усмотрению не нужно. **Выбор инструмента есть решение
> архитектора, а не вывод из наличия папки в шаблоне.**»

Практическое следствие: «в шаблоне есть X» — не аргумент в пользу X. Наличие папки описывает шаблон,
а не этот проект.

---

## 2. Минимально достаточный набор — светофор запуска

**Пока в этой таблице есть хоть одна строка ⛔, шаг с кодом не начинается.** Это исполнение закона §1,
а не осторожность.

| # | Без чего разработку начинать нельзя | Состояние |
|---|---|---|
| 1 | **Природа продукта** — что он делает | ✅ известна, §3 |
| 2 | **Мера успеха** — по чему понимают, что продукт работает | ✅ **закрыто 2026-08-24** (Рома): работают ОБА пути — мгновенный триггер и расписание. §3.2 |
| 3 | **Кто отправляет сообщения** | ✅ ChatPush, назван Ромой. §4.2 |
| 4 | **Режим отправки** — чей аккаунт, какие каналы | ✅ **закрыто 2026-08-24** (Рома): берём подключение, которое **уже есть** в приложении Филиппа; сервис временный. §4.2 |
| 5 | **Кто даёт данные** — какая CRM и по какому API | ✅ **закрыто 2026-08-24 исследованием исходника: YCLIENTS.** Доказательство — в §4.1, не «правдоподобно», а по коду, схеме и переменным окружения |
| 6 | **Чем будится цепочка** | ✅ **закрыто 2026-08-24** (Рома): схемы готовых автоматизаций в настройках, администратор их настраивает, скрипт под каждую создаётся ИИ и работает в фоне. §3.3 |
| 7 | **Одна клиника или несколько** | ✅ **закрыто 2026-08-24** (Рома): **одно приложение — всегда одно учреждение.** §3.4 |
| 8 | **Персональные данные** | ✅ **закрыто 2026-08-24** (Рома), 🔒 **временно**: работу с регулятором в текущей версии не оптимизируем, осознанно и на свой страх и риск. §6.4 |

🟢 **Строк ⛔: ноль. Разработка по §1 разрешена.**

**Не блокирует старт, но ждёт своего шага:** частота и массовость рассылки (сколько сообщений за раз,
есть ли лимиты у ChatPush) · чем ChatPush будет заменён, когда «временно» кончится · публичная часть
(нужна ли) · перенос данных LS centr · заводится ли продукт в панели и под каким `id`.

---

## 3. Продукт: что это

**Сервис рассылки сообщений в мессенджеры для повышения лояльности клиентов компании.**
📄 вводная Ромы, план шага 2.

🔒 **Ядро — цепочки событий, срабатывающие по таймеру при определённых условиях.** 📄 вводная Ромы,
план шага 1, дословно:

> «Природа проекта — не интерфейс. Это сервис рассылки сообщений в мессенджеры для повышения
> лояльности клиентов компании, а его ядро — **цепочки событий, срабатывающие по таймеру при
> определённых условиях**.»

> **«"Миграция удалась" означает не "интерфейс перенесён", а "таймерные цепочки работают на своём
> сервере со своей базой".** Карта, описавшая страницы и не назвавшая таймерные цепочки ядром, приёмку
> не проходит.»

**Откуда проект берётся.** Это перенос работающего прототипа: `autozabota-starter` (`carecrm`),
написан Филиппом, доведён до состояния полноценного прототипа на Vercel. 📄 рабочая инструкция §1.

**Причины переезда — не архитектурные** (📄 вводная Ромы, план шага 1):

1. Vercel не поддерживает работу с cron-событиями;
2. требования регулятора: данные нельзя хранить на зарубежных ресурсах — нужен свой сервер и своя база;
3. цена.

### 3.1. Как продукт устроен целевым образом

📄 Ответ Ромы, 2026-08-24. **Это описание того, как должно быть, а не того, что построено.**

```
человек посетил учреждение → попал в CRM
        │
        ▼
интеграция по API ПО ТАЙМЕРУ проверяет обновления
        │  приносит изменившиеся состояния
        ├──────────────────────────┬──────────────────────────
        ▼                          ▼
  МГНОВЕННЫЙ триггер          триггер ПО РАСПИСАНИЮ
  состояние в CRM             для аккаунта пользователя
  изменилось, и сценарий      наступило запланированное
  на это изменение отработал  событие
        └──────────────┬───────────┘
                       ▼
        событие передаёт состояние во внешний
        инструмент отправки → человек получает
        сообщение в мессенджер
```

⚠️ **Опрос CRM по таймеру тоже надо строить.** ✅ Проверено в шаге 1: в исходнике синхронизация с CRM
запускается **руками** (`node scripts/sync-yclients.js <salon_id>`), таймера нет ни одного. То есть
таймеров в цепочке два — опрос CRM и расписание событий, — и оба отсутствуют сегодня.

### 3.2. Мера успеха

🔒 **Ответ Ромы, 2026-08-24, дословно:**

> **«Мера успеха — работают ОБА пути.** Продукт считается работающим, когда уведомление получают и те,
> кто недавно попал в мгновенный триггер, и те, кто попал в рассылку по расписанию. **Один путь без
> второго — половина продукта.**»

Следствие для приёмки любого шага, касающегося ядра: доказательство обязано покрывать **оба** пути.
Работающий мгновенный триггер при неработающем расписании — не «почти готово», а половина.

📄 Для сведения: в исходнике главной цифрой была «возвращённая выручка» — сумма визитов в течение
30 дней после контакта по задаче (карта шага 1, §4). Это решение Филиппа; мерой успеха этого проекта
оно не является.

### 3.3. Чем будится цепочка — механизм назначен

🔒 **Ответ Ромы, 2026-08-24, дословно:**

> «В настройках приложения должны быть **схемы готовых автоматизаций, которые настраивает
> администратор**. Для каждой из них **при помощи искусственного интеллекта создаётся скрипт
> автоматизации**, работающий **в фоновом режиме**.»

> 🔒 «Это возможно именно потому, что **сервер свой (Ubuntu), а не Vercel**» — и это одна из названных
> причин переезда: Vercel на бесплатном тарифе не даёт надёжного крона.

**Что отсюда следует, и это надо держать в голове с первого шага кода:** администратор настраивает не
код, а **схему**; код под схему **порождается**. Значит у схемы обязаны быть форма, хранение и
валидация, а у порождённого скрипта — место, где он живёт, и способ его остановить. Ни того ни другого
сегодня нет (§5).

⛔ Не назначено и в этот подшаг не входило: где физически живёт фоновый процесс, чем он запускается на
Ubuntu, что происходит со скриптом при повторной генерации.

### 3.35. Тип приложения, роли и поток — решение Ромы, 2026-08-24

🔒 **Тип приложения: `scheduler`** — «Автоматизация по расписанию» из каталога 22 направлений
(`config/project-types.ts`). Определение каталога описывает ядро дословно: «Служба, которая сама
просыпается в назначенный час, ходит куда велено, сравнивает найденное с условием и присылает
результат. Решать ей нечего: где нужно суждение — там агент, а здесь правило».

Вторая вычитка — `agents` — рассмотрена и отклонена: у агентной автоматизации признак «следующий шаг
зависит от предыдущего», а здесь ИИ порождает скрипт один раз, работает же правило по расписанию.
Интерфейс при этом `crm`-образный («рабочее место отдела… нет посетителей, есть сотрудники с разными
правами») — тип один, экраны приходят из этой второй природы.

🔒 **Ответ Ромы, дословно** (пунктуация авторская, речь надиктована):

> «тип scheduler подтверждаю, роли: operator → manager, admin → admin, user and vip_user... Значит с
> точки зрения приложения нас будет такой поток: главная страница в принципе нам не интересно и на ней
> достаточно оставить всего лишь навсего бейджи с текстом авто забота А ниже будет просто заголовок
> название компании который подключится пока по умолчанию пусть остаются Fractera. И идее в том что
> любой человек который зайдёт по этой ссылке он увидит эту надпись единственная кнопка которая там
> будет эту кнопку войти больше на главной странице ничего не будет поэтому как мы понимаем эта
> страница доступна для не зарегистрированных пользователей дальше будет авторизация и следующие вещи:
> – пользователь после авторизации пойдёт на какую-то страницу благодарности с текстом спасибо что
> зарегистрировались скоро с вами свяжется наш менеджер., Пользователь который был переведён к статусу
> VIP получит страницу заглушку но уже приветственную заглушку которую скажет: здравствуйте фамилия имя
> приветствуем вас на сервисе автоботы. Этих страниц сейчас в проекте нету они по сути представляют из
> себя заглушки следующую роль эта роль администратора администратор получает возможность доступа по
> всем функциям программы втором числе настройке через пользовательский интерфейс данных компаний
> сейчас тоже этого настройках нету но сейчас у нас есть интерфейс менеджера и мы добавляем роль
> менеджера который в принципе настраивает опции проехать то есть все что сейчас есть в исходники
> Филиппа в условно защищенном протоколе это все что настраивает менеджер. Вот такие роли»

**Моё прочтение — это НЕ его слова, а их раскладка на архитектуру.** Всё, что ниже, проверяемо и при
расхождении с цитатой выше побеждает цитата.

| Роль | Что видит | Группа прав | Построено? |
|---|---|---|---|
| гость | главная: бейджи «Автозабота», заголовок с названием компании, **одна кнопка «Войти»** — и больше ничего | `(publicLayer)` | ⛔ сегодня на главной витрина стартера |
| `user` | страница благодарности: «спасибо что зарегистрировались, скоро с вами свяжется наш менеджер» | `(account)` | ⛔ заглушки нет |
| `vip_user` | приветственная заглушка: «здравствуйте <фамилия имя>, приветствуем вас на сервисе Автозаботы» | `(account)` | ⛔ заглушки нет |
| `manager` | **весь рабочий интерфейс исходника Филиппа** — «всё, что сейчас есть в исходнике Филиппа в защищённом протоколе, это всё, что настраивает менеджер» | `(staff)` | ⛔ переносится шагами |
| `admin` | «доступ по всем функциям программы», в том числе **настройка данных компаний через интерфейс** | `(admin)` | ⛔ настройки компаний нет |

✅ **Роли изобретать не пришлось:** `user`, `vip_user`, `manager`, `admin` — все четыре уже объявлены в
`lib/roles.ts`, а `user`/`vip_user` уже состоят в группе `(account)`.

✅ **Название компании — значение конфига, а не код.** `short_name` в `APP-CONFIG` уже `Fractera`,
поэтому «пока пусть остаётся Fractera» исполняется тем, что этого файла не трогают.

⚠️ **Прежнее предположение агента, что группа `(account)` продукту не нужна, — ОШИБОЧНО и снято.**
Оно строилось на исходнике, где пациенты не входят в систему. У Ромы входящие пользователи есть, и
`(account)` — их слой.

### 3.36. Три уточнения к ролям — решения Ромы, 2026-08-24

Три вопроса, поднятых предыдущим разделом, закрыты в тот же день. **Дословно:**

**① Права администратора — вариант B.**

> «Права администратора — вариант B. Решение от 2026-08-11 остаётся в силе, `lib/roles.ts` не трогаем.
> У администратора своя область: данные компаний и сотрудники. Рабочие экраны остаются менеджерскими.
> Работы по этому пункту ноль.»

✅ Проверено: `admin` в группе `staff` не состоит, и теперь это подтверждённое состояние, а не
столкновение. Закон владельца от 2026-08-11 остаётся в силе. **Работы ноль.**

**② Регистрация.**

> «На главном экране есть только кнопка «Войти», которая открывает стандартное окно авторизации.
> Выбор между входом и регистрацией происходит уже внутри окна авторизации. Отдельной страницы
> регистрации мы не строим: это слой авторизации платформы, а не наше приложение. Ничего своего здесь
> не изобретай.»

**③ Кто делает клиента VIP.**

> «Кто делает клиента VIP — оператор, и механизм переносится из исходника. В интерфейсе исходника у
> оператора есть кнопка «добавить пользователя». По нажатию оператор попадает в окно, где может
> редактировать пользователей и назначать роль `vip_user`.»

> ⚠️ «Назначение этой сущности владелец честно назвал неясным: непонятно, зачем системе пользователь,
> который не пришёл из CRM. Решение владельца — механизм построить, потому что кнопка в исходнике
> есть, а назначение выяснится позже.»

🛑 **Открытый вопрос, поднятый самим Ромой и обязательный к выносу при постройке этого экрана.**
Его словами:

> «В ответе владельца две половины спорят между собой: пользователь «никак не будет связан с системой
> авторизации» — и одновременно роль `vip_user` нужна, «чтобы этот пользователь попал в систему».
> Роль без учётной записи входа не даёт. Значит либо такой пользователь — это запись внутри приложения
> без входа вообще (и тогда роль ему не нужна), либо ему всё-таки заводится учётная запись в слое
> авторизации. Владелец сам сказал, что назначение неясно, — это открытый вопрос, а не оплошность.
> Подними его в тот момент, когда дойдёшь до постройки этого экрана, покажи обе вычитки и цену каждой.
> Не выбирай молча.»

⚠️ **Второе расхождение, найденное агентом при проверке пункта ③ — вынесено, не решено.**
Решение «механизм построить» в силе; под сомнением только слова «переносится из исходника».
✅ Проверено в исходнике — описанной кнопки там нет, а есть **две разные, и ни одна не та**:

| Что в исходнике есть | Где | Кому видна | Назначает роли? |
|---|---|---|---|
| «**Добавить пациента**» | `/patients` | **обеим** ролям | нет: пациент — не учётная запись, поля роли у него нет вовсе |
| «**Добавить сотрудника**» | `/users` | **только админу** (`adminOnly` в меню + `role !== "admin" → forbidden()` в двери) | да, но **только `admin` / `operator`** |

И третье: **роли `vip` в исходнике нет ни одной строкой** (поиск по всему дереву — ноль совпадений).

**Что из этого следует.** Экран «оператор назначает `vip_user`» — не перенос, а **новая постройка по
мотивам двух разных кнопок**, и вдобавок она расширяет право: в исходнике роли назначает админ, а
здесь их назначает оператор. По `use-roles` §4 расширение права требует названного противовеса.
Обе вычитки и цена каждой поднимаются вместе с вопросом выше — **в момент постройки экрана**, не раньше.

### 3.37. `vip_user` — это КЛИЕНТ клиники. Решение Ромы, 2026-08-24

🔒 **Решение владельца, дословно:**

> «`vip_user` — это **КЛИЕНТ клиники**.»

Этим снимается неясность, названная в §3.36: сущность, «которая не пришла из CRM», получает имя.
Три следствия — тоже решения владельца, дословно:

> **«1. Это не перенос, а новая способность.** В исходнике её нет ни в каком виде — я проверил дверь
> `app/api/users/route.js`: она открыта только роли `admin`, требует пароль, принимает лишь роли
> `admin` и `operator` и создаёт учётную запись входа. К клиентам отношения не имеет. Ничего похожего
> переносить не из чего.»

> **«2. Появляется личный кабинет клиента.** Роли `user` и `vip_user` живут в группе `(account)`, обе
> сегодня пусты — страница благодарности и приветственная заглушка. Содержание кабинета придёт
> отдельным решением владельца.»

> **«3. Расширение права требует противовеса.** В исходнике роли назначал только `admin`; владелец
> решил, что VIP назначает оператор. Это расширение, и по `use-roles` оно записывается явно, а не
> растворяется в группе.»

✅ **Проверено агентом независимо — все три опоры сходятся:**

| Утверждение | Чем подтверждено |
|---|---|
| дверь `users` — только `admin`, с паролем, роли только `admin`/`operator`, создаёт учётку | `app/api/users/route.js:8,19` (`role !== "admin"`), `:22,25` (пароль, мин. 6), `:26` (`["admin","operator"].includes`), `:32` (`INSERT INTO users … password_hash`) |
| у КЛИЕНТА телефон обязателен, email — нет | `scripts/schema.sql:20` `phone TEXT NOT NULL`, `:21` `email TEXT` |
| у СОТРУДНИКА наоборот, email обязателен | `scripts/schema.sql:11` `email TEXT NOT NULL UNIQUE` — разные таблицы, разные обязательности |

### 🛑 Открытый вопрос кабинета клиента — поднять при постройке экрана, не решать молча

**Механизм расхождения.** Клиенты приходят из CRM, где **телефон обязателен, а email может
отсутствовать**. Слой авторизации Fractera входа по телефону **не имеет**.

✅ **Проверено чтением службы на сервере** (`/opt/fractera/services/auth`), и точнее, чем «телефона
нет»: дверь `app/api/auth/methods/route.ts` объявляет ровно два дополнительных способа входа —

```json
{ "google": …, "magicLink": … }
```

— поверх базового email + пароль. Ни телефона, ни SMS, ни одноразовых кодов: строгий поиск по
`phoneNumber|phone_number|signIn.*phone|smsProvider|twilio` даёт ноль. 🔒 **И все три способа опираются
на email** — даже вход через Google возвращает почту. Значит:

- связать аккаунт клиента с его карточкой можно **только по email**;
- **клиент без email не войдёт вовсе.**

**Три вычитки, каждую показать владельцу с ценой:**

| | Вычитка | Цена |
|---|---|---|
| **A** | вход по email; клиенты без почты остаются вне кабинета | дёшево и работает завтра; но часть клиентской базы кабинета не получает никогда, и это надо уметь объяснить клинике |
| **B** | спрашивать email при первом входе | кабинет доступен всем; но появляется ещё один экран и вопрос, кто и как проверяет, что почта принадлежит именно этому клиенту |
| **C** | вход по телефону | закрывает случай честно; но это **изменение платформы, а не приложения** — заказывается у разработчиков Fractera, сроков мы не назначаем, работа встаёт |

🔒 **Не выбирать молча.** Владелец решает, когда дойдём до экрана.

### 3.4. Одно приложение — одно учреждение

🔒 **Ответ Ромы, 2026-08-24, дословно:**

> **«Одно приложение — всегда одно учреждение.** Клиника, или другой бизнес, но одно.»

Многоклиничности нет. Прямое следствие для переноса: колонка `clinic_id`, стоящая в исходнике в каждой
из 14 таблиц, **в результат не переезжает** — поле, которое всегда равно одному значению, это поле,
которое врёт (📄 карта шага 1, §7.3).

Второе следствие: «учреждение» шире клиники. Продукт не обязан говорить «пациент» — словарь предметной
области выбирается отдельно и в этом шаге не выбран.

---

## 4. Внешние службы

🔒 **Обе внешние. Ни одна не является частью платформы Fractera, и ни одна не заменяется её
инструментами** — см. закон §1 про «наличие инструмента не есть задание».

### 4.1. Поставщик данных — ✅ YCLIENTS, доказано

**Закрыто 2026-08-24, шаг 3-1.** Рома не назвал CRM поимённо намеренно — ответ добывался исследованием
исходника, а не вопросом. Ниже доказательство, а не правдоподобие: **четыре независимых следа**, каждый
проверяется одной командой.

| # | След | Чем проверено |
|---|---|---|
| 1 | **домен внешнего API прямо в коде** — `const API_BASE = "https://api.yclients.com"` | `lib/yclients.js:3` |
| 2 | **следы в СХЕМЕ БАЗЫ, а не только в коде**: `patients.yclients_client_id`, `visit_records.yclients_record_id`, таблицы `yclients_connections` / `yclients_events`, уникальный индекс `uq_patients_yclients` | `scripts/schema.sql:32,40,94,103,147` |
| 3 | **переменные окружения самого исходника**: `YCLIENTS_PARTNER_TOKEN`, `YCLIENTS_USER_TOKEN` — и **никакой другой CRM в файле нет** | `.env.local` исходника, перечень имён |
| 4 | **двенадцать файлов** в `app/`, `lib/`, `scripts/` обращаются к YCLIENTS; конкурирующего поставщика ни одного | поиск по дереву исходника |

Негативный контроль: в `.env.local` исходника **нет** переменных ChatPush — токен этого сервиса лежит в
таблице (§4.2). Значит перечень имён отражает реальный состав интеграций, а не всё подряд.

**Как подключается** (📄 из кода исходника):

- авторизация **двойная**, одним заголовком: `Authorization: Bearer <partner_token>, User <user_token>`;
  плюс обязательный `Accept: application/vnd.yclients.v2+json`;
- точки, которые исходник реально дёргает: `POST /api/v1/company/{salon_id}/clients/search`
  (постранично, `page_size=200`), `GET /api/v1/records/{salon_id}` (визиты за окно дат),
  `GET /api/v1/company/{salon_id}`, `POST /marketplace/partner/callback` (активация из маркетплейса);
- лимит YCLIENTS: 200 запросов/мин или 5/сек на IP — исходник держит паузу 250 мс между страницами;
- филиал: `salon_id 916279` (LS centr, косметология, Кисловодск).

🗣 **Со слов Филиппа, Ромой не проверено:** все пользователи, существующие в приложении, — реальные,
полученные из CRM через её интеграцию. ✅ Механизм этому соответствует: синхронизация пишет
`yclients_client_id`, а уникальный индекс не даёт завести двух карточек на одного клиента CRM. Но
**реальность самих данных кодом не доказывается** — для этого нужна база, к которой мы не обращались.

⚠️ **Замена CRM.** Ничто в ответах не обещает, что YCLIENTS вечен. Строить так, чтобы поставщик был
заменяем: доступ к CRM — за одной границей, а не размазан по двенадцати файлам, как в исходнике.

### 4.2. Отправка сообщений — ChatPush по API

🔒 **Решение Ромы, 2026-08-24, дословно:**

> **«Отправкой сообщений в этом проекте занимается сторонний сервис ChatPush, подключаемый по API.**
> Служба Telegram, найденная в шаблоне, к задаче отношения не имеет и кандидатом никогда не была.»

📄 Что известно о ChatPush из исходника (карта шага 1, §3): аккаунт клиники `customer_id 26835`,
оба канала — WhatsApp и Telegram — были авторизованы и живы; отправка одним вызовом
`POST api.chatpush.ru/api/v1/delivery` с токеном в заголовке; входящие приходят вебхуком; токен хранится
**в таблице, а не в env** — задел автора исходника на несколько клиник.

🗣 **Со слов Филиппа, Ромой НЕ проверено** (📄 план шага 2, дословно): «отправка работает де-факто —
нажали, сообщение ушло в сторонний сервис, доставку он взял на себя, тестовый клиент уведомление
получил». **Пока не проверено — это не факт.**

🔒 **Ответ Ромы, 2026-08-24, дословно:**

> «Сервис используется **временно**. В приложении Филиппа подключение и интеграция **уже есть**: в
> тестовом режиме пользователи сообщения получают».

**Что это значит для работы.** Аккаунт и каналы не назначаются заново — берётся то подключение, что
уже работает у Филиппа. Строить при этом надо так, чтобы **замена шлюза не стоила переписывания
продукта**: слово «временно» сказано владельцем прямо, и его нельзя забыть к тому шагу, где отправка
будет строиться.

⚠️ Не блокирует старт, но выяснить к шагу отправки: массовая рассылка или по одному, ограничения по
частоте на стороне ChatPush, чем сервис будет заменён.

### 4.3. Модель (AI)

📄 В исходнике: любой OpenAI-совместимый провайдер, сочиняет черновик сообщения и ответ клиенту;
🔒 без ключа и при ошибке провайдера работает встроенный офлайн-генератор — работа не останавливается.
✅ В результате есть ключ `OPENAI_API_KEY` (📄 рабочая инструкция §7).

⛔ **Открыто:** нужен ли AI в результате вообще и в какой роли. Наличие ключа — не задание (§1).

---

## 5. Что в проекте есть сегодня, а чего нет

### Есть — проверено машиной

| Что | Чем проверено |
|---|---|
| ✅ `fractera-light` **1.2.2**, Next **16.2.0**, React **19.2.3** | `package.json` |
| ✅ Домен подключён: `https://aifa.dev`, слой данных `https://data.aifa.dev` | `APP-CONFIG/app-config.json`, `.env.local` |
| ✅ Языки: `en,ru`, по умолчанию **ru** | `NEXT_PUBLIC_SUPPORTED_LANGUAGES`, `NEXT_PUBLIC_DEFAULT_LOCALE` |
| ✅ `DESIGN-CONFIG` заполнен (488 байт, акцент `#16a34a`) | `DESIGN-CONFIG/design-config.json` |
| ✅ Учёт шагов на месте, параллельного учёта нет | `npm run check:steps` → `===STEPS_OK===` |

### Нет — проверено машиной

| Чего нет | Чем проверено | Что это значит |
|---|---|---|
| ⛔ **Ни календаря, ни таймеров в приложении** | 🗣 Рома, план шага 2, дословно: «В приложении сегодня нет ни календаря, ни таймеров» | ядро продукта (§3) не построено ни на сколько |
| ⛔ **Продукт в панели не заведён** | `PRODUCTS-CONFIG/` содержит только `README.md`, `defaults.json`, `schema.json` — нет ни досье `<id>.json`, ни `registry.json` | вечного `id` нет, а на нём висят имена таблиц (`<id>_*`) и пути логики |
| ⛔ **`PLATFORM-CONFIG` пуст** (`{}`, 4 байта) | `PLATFORM-CONFIG/platform-config.json` | режим разработки не выбран → по умолчанию `classic`; одиннадцать переключателей на значениях по умолчанию |
| ⛔ **Нет схем автоматизаций** — ни формы, ни хранения, ни валидации; нет и места, где жил бы порождённый ИИ скрипт | механизм назначен Ромой (§3.3), построенного нет | это и есть ядро продукта |
| ⛔ **Нет опроса CRM по таймеру** | ✅ в исходнике синхронизация ручная (шаг 1); в результате интеграции нет вовсе | таймеров в целевой схеме два, отсутствуют оба |
| ⛔ **Ни одной своей таблицы, страницы или двери продукта** | карта шага 1 | переносить ещё не начинали |

### Что приехало в шаблоне и заданием НЕ является

По закону §1 перечисляю **как состав шаблона, а не как план**: образцы витрин товаров во всех четырёх
группах прав, блог, footer-страницы, статический виджет `security-orbit`, пять инструментов в
`_tools/`, продукт-образец Telegram Desk (`tgdesk_*`, двери `/api/telegram/*`), служба каналов на
`:3500`. Применение любого из них к Автозаботе — **решение архитектора**, а не вывод из наличия папки.

🔒 **Порядок работы с этой избыточностью — решение Ромы, дословно** (📄 план шага 1):

> «Стартер богат: часть его маршрутов задаёт правильные паттерны, часть для этого проекта избыточна.
> **Сначала создаются нужные маршруты и структуры в них, и только потом удаляются предустановленные по
> умолчанию.** Не наоборот.»

---

## 6. Правила проекта

### 6.1. Что можно и чего нельзя с исходником

📄 Рабочая инструкция §8, решение Ромы 2026-08-24: исходник Филиппа **не запускаем и не хостим**,
домен под него не заводим, после миграции он умирает. Его env-переменные — только описание интеграций.
Данные из его баз извлекаем сами, на этапе проверки интеграций. **Проверка любой работы — только на
боевом сервере**, локальные прогоны исходника не нужны.

🔒 Отдельно (📄 корпус Fractera): чужой код **никогда не переписывается — он читается как описание**.
Несовместимый стек не препятствие: переезжает смысл возможности, а не файлы.

### 6.2. Язык

📄 Рабочая инструкция §3: общение с Ромой — только по-русски. Приложение мультиязычное, русский +
английский, **по умолчанию русский** (✅ подтверждено `.env.local`). Временное упрощение: в оба языка
заливается русский текст, замена на английский — задача будущего этапа.

### 6.3. Технические запреты

📄 Рабочая инструкция §5: **только Turbopack, Webpack запрещён абсолютно.** Платформа — Windows 11,
PowerShell + Git Bash.
📄 `ANTI-PATTERNS.md`: **`middleware.ts` создавать нельзя** — гейт живёт в `proxy.ts`, это осознанное
соглашение проекта, а не дефект «на починку».

### 6.4. Персональные данные

📄 Причина переезда №2: регулятор запрещает хранить данные за рубежом. 📄 Корпус (`use-personal-data`):
человек и его дело — **две таблицы**, `<entity>_people` не покидает сервер никогда, наружу уходит
обезличенный `person_id` и минимум фактов, соединение обратно — только здесь.

🔒 **Решение Ромы, 2026-08-24, дословно — и оно ВРЕМЕННОЕ:**

> **«В текущей версии работу с регулятором мы не оптимизируем, осознанно и на свой страх и риск.»**
> Работаем с теми данными, которые приходят; предположительно CRM отдаёт фейковые данные для
> тестирования.

🔒 **Записано именно так, чтобы позже это не приняли за постоянное.** Это решение текущей версии, а не
закон продукта. Признаки того, что срок вышел: в базу поехали настоящие пациенты вместо тестовых, или
продукт увидел кто-то кроме нас. Тогда §6.4 переписывается, а не «вспоминается».

**Чего решение НЕ отменяет.** Требование регулятора — одна из трёх названных причин переезда (§3), и
хранение данных на своём сервере как раз ему и служит. Отменена **оптимизация под регулятора**
(расщепление таблиц, обезличивание наружу), а не сам переезд.

📄 Замечание из карты шага 1 остаётся в силе как инженерное: исходник **отправлял имя пациента в
OpenAI**. Приём не повторяется — не из-за регулятора, а потому что имя модели не нужно.

---

## 7. Окружение: правило вычисления адреса

🔒 **Решение Ромы, 2026-08-24, дословно:**

> «Значение, передаваемое другой машине, вычисляется **с точки зрения ТОЙ машины** и никогда не
> копируется из конфигурации сервера.»

Адресов у слоя данных три, и верный выбирается не предпочтением, а тем, **какая машина читает файл**
(📄 план шага 2):

| Адрес | Когда верен | Поток |
|---|---|---|
| `https://data.<домен>` | любая машина снаружи сервера, домен подключён | **защищённый**: сертификат, порт не участвует |
| `http://<IP сервера>:3300` | любая машина снаружи, домена нет | **незащищённый**: IP и порт открыто, компромисс режима без домена |
| `http://localhost:3300` | **только сам сервер** | внутримашинный; на ноутбуке указывает на сам ноутбук и ведёт в пустоту |

**В этом проекте** ✅ `FRACTERA_IP_NODOMAIN_MODE=false` (`.env.local:25`) — режим доменный, значит
верное значение **`https://data.aifa.dev`** (строка 9).

**Дефект выгрузки, а не переключатель режимов.** 📄 Объяснение Ромы: панель дописывала собственный ключ
второй раз серверным значением; `.env` читается сверху вниз, побеждает последняя строка — правильный
адрес молча отменялся своим же дублем. В панели починено; **на сервере стоит сборка до починки, поэтому
при следующей выгрузке дубль вернётся.**

✅ **Проверено машиной, и дефект шире, чем сообщалось:** дублируются **два** ключа, не один.

```
$ grep -o '^[A-Z_][A-Z0-9_]*=' .env.local | sort | uniq -d
DATA_SECRET=
REMOTE_DATA_URL=
```

`REMOTE_DATA_URL` — строки 9 и 24, значения **разные** → вредит. `DATA_SECRET` — строки 10 и 23,
значения **совпадают** (обе длиной 64) → сегодня безвреден. Совпадение значений у секрета — лишнее
подтверждение диагноза: переключатель режимов не стал бы дублировать секрет тем же значением.

🔒 **`.env.local` не правим — файл трогает Рома.**

---

## 8. Что проверено, а что принято со слов

| Утверждение | Достоверность |
|---|---|
| Доменный режим, верный адрес слоя данных — `https://data.aifa.dev` | ✅ проверено: `.env.local:9,25` |
| Дублируются два ключа, у `REMOTE_DATA_URL` значения разные, у `DATA_SECRET` одинаковые | ✅ проверено: `uniq -d`, сравнение значений |
| Версии, языки, состояние четырёх конфигов, отсутствие досье продукта | ✅ проверено: `package.json`, конфиги, `ls PRODUCTS-CONFIG` |
| В исходнике нет ни одного таймера; отправка ручная | ✅ проверено в шаге 1: `grep` на планировщик → 0 строк при негативном контроле в 4 строки; плюс README исходника двумя абзацами |
| Отправку делает ChatPush, сервис **временный** | 📄 решения Ромы, планы шагов 2 и 3 |
| **Поставщик данных — YCLIENTS** | ✅ **доказано 2026-08-24** четырьмя следами: домен в коде, следы в схеме БД, переменные окружения исходника, двенадцать файлов без конкурента (§4.1) |
| Мера успеха — работают оба пути; одно приложение = одно учреждение; цепочку будят схемы автоматизаций со скриптом от ИИ; регулятора временно не оптимизируем | 📄 ответы Ромы, план шага 3, записаны дословно |
| «Отправка работает де-факто, тестовый клиент уведомление получил» | 🗣 **со слов Филиппа, Ромой НЕ проверено** |
| «Все пользователи в приложении — реальные, из CRM» | 🗣 **со слов Филиппа, Ромой НЕ проверено.** ✅ Механизм этому соответствует (`yclients_client_id` + уникальный индекс), но реальность данных кодом не доказывается |
| Опрос CRM по таймеру в исходнике отсутствует — синхронизация ручная | ✅ проверено в шаге 1: `grep` на планировщик → 0 строк; запуск `node scripts/sync-yclients.js <salon_id>` |
| «В приложении нет ни календаря, ни таймеров» | 🗣 Рома, план шага 2 |
| Размещение экранов по группам прав из карты шага 1 | 📄 предложение агента, ничем не подтверждено |

---

## 9. Открытые вопросы

🟢 **Блокирующих не осталось: 2026-08-24 закрыты все шесть.** Пять — ответами Ромы, шестой (CRM) —
исследованием исходника. Разработка по §1 разрешена.

| # | Вопрос | Когда нужен ответ |
|---|---|---|
| 0 | 🟢 **ЗАКРЫТО 2026-08-24.** Как клиент попадает в кабинет: **вход только по email** (вычитка A). Связывание email с телефоном Рома вынес **за пределы задачи миграции** — вычитка C (вход по телефону, изменение платформы) не отвергнута, а отложена. Клиент без почты в кабинет не попадает, и это принятая цена. §11.6 | закрыто |
| 1 | Где физически живёт фоновый процесс автоматизаций, чем запускается на Ubuntu, что делать со старым скриптом при повторной генерации ИИ | к первому шагу по ядру |
| 2 | Массовость и частота рассылки; лимиты ChatPush | к шагу отправки |
| 3 | Чем ChatPush будет заменён, когда «временно» кончится | к шагу отправки |
| 4 | Как хранится и валидируется «схема автоматизации», которую настраивает администратор | к первому шагу по ядру |
| 5 | Заводится ли продукт в панели, под каким `id` — от этого зависят имена таблиц и пути логики | к первому шагу с таблицами |
| 6 | Словарь предметной области: «пациент» или нейтральное «клиент» — учреждение может быть не клиникой | к первому шагу с интерфейсом |
| 7 | Нужна ли публичная часть (витрина, страница подключения из маркетплейса) | к своему шагу |
| 8 | Нужен ли AI для текстов сообщений и чей | к своему шагу |
| 9 | Когда и как переносим данные LS centr (1815 пациентов, 5529 визитов на 2026-08-16) | последним шагом |
| 10 | Переиздать ли живые креды, попавшие в чат 2026-08-24 | висит с 2026-08-24 |

🔒 **Срок годности решения §6.4.** «Регулятора не оптимизируем» — решение текущей версии. Как только в
базе появятся настоящие люди вместо тестовых, вопрос возвращается сюда блокирующим.

---

## 10. Что этот файл НЕ содержит

Границы намеренные, чтобы паспорт не превратился во второй журнал:

- **не состояние работы** — оно в `current-steps.md`;
- **не итоги шагов** — они в `completed-steps/`;
- **не карта исходника** — она в `reports/migration-source-inventory-…md`;
- **не законы архитектуры** — они в `CLAUDE.md` и навыках;
- **не термины** — они в `GLOSSARY.md`.

Сюда попадает только то, чему негде больше жить: природа продукта, внешние службы, решения Ромы,
достоверность и открытые вопросы.

---

## 11. Архитектура маршрутов — предложение шага 6-2

🟢 **СОГЛАСОВАНО Ромой 2026-08-24** — см. §11.9. По этой архитектуре можно начинать переезд функций.

### 11.1. Три причины, по которым структура отличается от исходника

1. **Группы маршрутов связаны с ролевой авторизацией.** Маршрут живёт в слое (`(publicLayer)` /
   `(protectedLayer)`) и в группе прав (`account` · `staff` · `admin` · `finance`). Раскладка ролей
   согласована в шаге 5 (§3.35–3.37).
2. **Архитектура мультиязычная всегда.** Языковой префикс стоит в адресе, даже когда язык один:
   владелец включает второй в любой момент, и адреса не переписываются. ✅ Сегодня набор `en,ru`.
   **Это единственное обязательное отличие адреса от исходника.**
3. **Появились маршруты, которых в исходнике нет вовсе** — из решений владельца: личный кабинет
   клиента (`user`, `vip_user`), экран назначения `vip_user` и настройка данных компании.

🔒 **Закон переноса адресов соблюдён: адрес сохраняется 1:1, кроме языкового префикса.**
✅ **Проверено, что это выполнимо:** правило «страница лежит двумя уровнями ниже слоя» сторож
`check:content` применяет к `(publicLayer)`, а не к защищённому; образец `(admin)/blocks` лежит одним
уровнем ниже группы прав и даёт адрес `/<lang>/blocks`. Значит `/patients` остаётся `/patients`, а не
превращается в `/manage/patients`.

⚠️ **Честно о том, ЗАЧЕМ он соблюдён.** Из двух причин закона первая — поисковая — здесь **не
работает**: ✅ `middleware.js:4` объявляет публичными только `/login`, `/api/auth/login`,
`/connect/yclients` и две двери интеграций, остальное уводится на вход, а `/` редиректит на
`/dashboard`. Индексировать нечего. **Адреса сохраняются ради второй причины — привычки операторов:**
закладки, ссылки в переписке, память тех, кто работал в системе.

### 11.2. Дерево предлагаемой архитектуры

```
app/[lang]/
│
├── (publicLayer)/                     видно всем, без входа
│   ├── ·                              /ru            главная: бейджи «Автозабота»,
│   │                                                 название компании, одна кнопка «Войти»
│   └── connect/yclients               /ru/connect/yclients     приём подключения из маркетплейса
│
└── (protectedLayer)/                  robots: index false
    │
    ├── (account)/                     КЛИЕНТ клиники — user · vip_user
    │   └── cabinet                    /ru/cabinet    заглушка: благодарность (user)
    │                                                 или приветствие по имени (vip_user)
    │
    ├── (staff)/                       МЕНЕДЖЕР — весь рабочий интерфейс исходника
    │   ├── dashboard                  /ru/dashboard
    │   ├── tasks                      /ru/tasks
    │   ├── patients                   /ru/patients
    │   │   └── [id]                   /ru/patients/[id]
    │   ├── messages                   /ru/messages
    │   ├── scenarios                  /ru/scenarios
    │   ├── services                   /ru/services
    │   ├── analytics                  /ru/analytics
    │   ├── audit                      /ru/audit
    │   └── clients                    /ru/clients    НОВОЕ: назначение роли vip_user
    │
    └── (admin)/                       АДМИНИСТРАТОР — своя область
        ├── administration/users       /ru/administration/users   учётные записи (образец построен)
        └── company                    /ru/company    НОВОЕ: данные компании
```

Группа `(finance)` не используется: по решению Ромы весь рабочий интерфейс, включая «Аналитику» с
выручкой, принадлежит менеджеру.

### 11.3. Построчный разбор: каждому маршруту исходника — одно решение

| Маршрут исходника | Решение | Новый адрес · роль · сущность |
|---|---|---|
| `/dashboard` | **1:1** | `/ru/dashboard` · `manager`, `(staff)` · `visit_records`, `care_tasks`, `care_scenarios`, `messages` |
| `/tasks` | **1:1** | `/ru/tasks` · `manager` · `care_tasks`, `patients`, `care_scenarios` |
| `/patients` | **1:1** | `/ru/patients` · `manager` · `patients`, `visit_records`, `care_tasks` |
| `/patients/[id]` | **1:1** | `/ru/patients/[id]` · `manager` · плюс `activity_logs`, `messages`, `service_protocols` |
| `/messages` | **1:1** | `/ru/messages` · `manager` · `messages`, `patients` |
| `/scenarios` | **1:1** | `/ru/scenarios` · `manager` · `care_scenarios` — здесь живут правила цепочек, ядро продукта |
| `/services` | **1:1** | `/ru/services` · `manager` · `service_protocols`, `visit_records` |
| `/analytics` | **1:1** | `/ru/analytics` · `manager` · `visit_records`, `patients` |
| `/audit` | **1:1** | `/ru/audit` · `manager` · `visit_records`, `patients` |
| `/connect/yclients` | **1:1** | `/ru/connect/yclients` · **публичный** · `yclients_connections`, `yclients_events`; двери `/api/yclients/*` называются в `PUBLIC_API_PREFIXES` тем же правком, что их создаёт |
| `/` | **меняем содержимое, адрес тот же** | `/ru` · публичный. Было: редирект на `/dashboard`. Станет: бейджи, название компании, кнопка «Войти» — решение Ромы. Собирается каталожными видами (`badges`, `statement` — ✅ оба есть), то есть списком блоков, а не новым кодом |
| `/login` | **не переносим** | вход живёт в службе `:3001`; выбор «вход или регистрация» внутри её окна — решение Ромы. Место занимает кнопка «Войти» на главной |
| `/users` | **меняем адрес** → `/ru/administration/users` | `admin`, `(admin)`; записи живут в службе `:3001`, своей таблицы `users` нет. **Причина:** такой экран уже построен в шаблоне как образец «чужие данные за дверью службы»; второй адрес для той же сущности — два экрана одного назначения. Вторая вычитка ниже |

**Новые маршруты, которых в исходнике не было:**

| Адрес | Из какого решения владельца | Состояние |
|---|---|---|
| `/ru/cabinet` | «`vip_user` — КЛИЕНТ клиники», «появляется личный кабинет клиента» (§3.37) | заглушка; содержание кабинета — отдельное решение владельца |
| `/ru/clients` | «VIP назначает оператор» (§3.36) | ⛔ форма зависит от нерешённого вопроса кабинета (§9, строка 0) |
| `/ru/company` | «у администратора своя область: данные компаний и сотрудники» (§3.36) | в исходнике нет ни в каком виде |

### 11.4. Две вычитки, которые агент не стал решать молча

**① Адрес учётных записей.**

| | Вычитка | Цена |
|---|---|---|
| **A** | `/ru/users` — буква закона переноса | появляется второй экран учётных записей рядом с уже построенным образцом |
| **B** ⭐ | `/ru/administration/users` — переиспользуем построенное | адрес меняется сверх языкового префикса |

Довод за **B**: живая причина закона здесь — привычка операторов, а `/users` в исходнике был **скрыт
от оператора** (`adminOnly` в меню, `role !== "admin"` в двери). Привычки, которую надо беречь, у этого
адреса нет.

**② Кабинет клиента — одна страница или две.** Рома дважды сказал «страницу»: благодарность для `user`
и приветствие для `vip_user`. Предлагается **один маршрут `/ru/cabinet` с двумя состояниями по роли** —
обе заглушки, содержание придёт отдельным решением. Вторая вычитка: два разных адреса. Разница
проявится, когда кабинет наполнится.

### 11.5. Чего агент сделать не может

1. **Куда служба авторизации возвращает после входа и можно ли развести по ролям** — `user` на
   `/cabinet`, `manager` на `/dashboard`. Это настройка слоя авторизации, а не приложения.
2. **Содержание кабинета клиента** — владелец сам назвал это отдельным решением.
3. **Форма экрана `/ru/clients`** — упирается в нерешённый вопрос: как клиент попадает в кабинет, если
   у него телефон и может не быть email (§9, строка 0).
4. **Имена таблиц продукта** — по закону они `<id>_*` от вечного `id` из досье, а продукт в панели не
   заведён: `PRODUCTS-CONFIG` пуст.

### 11.6. Ответы Ромы на §11.5 — решения владельца, 2026-08-24

🔒 **Дословно:**

> «Ответ на вопрос один означает что пользователь должен получить динамический маршрут и быть возвращен
> на свой собственную страницу а именно обычный пользователь получит приветствие сообщения упоминанием
> о том что у вас право юзер, VP юзер попадёт в кабинет с приветствием сообщением что он VIP, а менеджер
> попадёт на дашборд ну на самом деле я думаю что именно так и работала бы в старое приложения всё
> оставляю.
> Два содержание кабинета минимальная заглушка
> Три сейчас форма авторизации только e-mail а электронный адрес связывается с телефоном за пределами
> текущей задачей миграции.
> Четыре первую часть вопроса я не понимаю то что касается то что продукт не заведёт в CONFIG это не
> нужно беспокоить мы работаем в классической разработки это знаешь что продукты может оставаться в
> CONFIG пустой записи.»

**Что из этого следует — раскладка агента, не его слова.**

| # | Решение | Следствие для работы |
|---|---|---|
| 1 | после входа человек попадает **на свою страницу по роли**: `user` — приветствие с упоминанием права «юзер», `vip_user` — кабинет с приветствием VIP, `manager` — дашборд | развилка по роли, а не один общий адрес для всех |
| 2 | содержание кабинета — **минимальная заглушка** | наполнение кабинета в объём переезда не входит |
| 3 | **вход только по email**; связывание email с телефоном — **за пределами задачи миграции** | 🟢 закрывает открытый вопрос §9 строка 0: берётся вычитка **A** |
| 4 | продукт в `PRODUCTS-CONFIG` **не заводится**; работаем в классической разработке, запись остаётся пустой | имена таблиц не могут висеть на `<id>` из досье — см. ниже |

🟢 **Открытый вопрос кабинета закрыт.** Из трёх вычиток (§3.37) выбрана **A**: вход по email; клиент
без почты в кабинет не попадает. Вычитка **C** (вход по телефону, изменение платформы) не отвергнута
навсегда — она **вынесена за границы переезда**.

### 11.7. Что означал §11.5 пункт 4 — и как решается

Вопрос был непонятен, потому что я назвал закон, не объяснив его. Закон такой: у продукта есть вечный
`id` из досье (`p1`, `p2`), и таблицы продукта называются `<id>_*` — `p1_tasks`, `p1_patients`. Смысл:
`id` ничего не значит и никогда не меняется, а имя и адрес владелец правит свободно, поэтому имя
таблицы, выведенное из чего-то изменяемого, однажды соврёт.

Досье заводится в панели. Рома решил: **не заводим, работаем классически, запись остаётся пустой.**
Значит вешать таблицы не на что.

🔒 **Отступление уже предусмотрено архитектурой, изобретать не нужно.** В шаблоне ровно тот же случай
у продукта-образца Telegram Desk, и там записано дословно: «ПРЕФИКС `tgdesk_`, А НЕ `<id>_`. Закон
продуктов велит называть таблицы по вечному `id` из досье, но у образца досье ещё нет… Отступление
названо вслух и живёт ровно до регистрации; зарегистрировал — таблицы переименовываются шагом, а не
молча» (`lib/db/index.ts`, `SCHEMA`).

**Предложение агента:** таблицы получают именной префикс **`care_`** — тот же, что несёт исходник
(`care_scenarios`, `care_tasks`), то есть слово, уже принятое в предметной области продукта. Отступление
называется вслух здесь и живёт ровно до регистрации продукта в панели.

### 11.8. Одна вычитка, оставшаяся неназванной

«Пользователь должен получить **динамический маршрут**» читается двумя способами:

| | Вычитка | Цена |
|---|---|---|
| **A** ⭐ | развилка решается **на входе по роли**, адреса обычные: `user`/`vip_user` → `/ru/cabinet`, `manager` → `/ru/dashboard` | ничего нового в модели данных; совпадает с «именно так и работало бы старое приложение» |
| **B** | у каждого своя страница с динамическим сегментом: `/ru/cabinet/[id]` | нужен идентификатор в адресе, а он персональные данные в URL — этого архитектура избегает |

**Взята A**, потому что она не заводит новой сущности и отвечает словам «всё оставляю». Если имелось
в виду **B** — скажи, это меняет адрес, но не объём.

### 11.9. Состояние раздела 11

🟢 **Считаю архитектуру СОГЛАСОВАННОЙ.** Основание: Рома ответил на все четыре пункта §11.5 и сказал
«всё оставляю», не возразив ни против дерева, ни против построчного разбора. По ней можно начинать
переезд функций.

⚠️ Если я прочитал «всё оставляю» шире, чем сказано, — поправь одной строкой, и раздел вернётся в
состояние предложения.
