// Макет карточки пациента.
//
// 🔒 ЗАЧЕМ ОН ЕСТЬ, ЕСЛИ НИЧЕГО НЕ РИСУЕТ. Он объявляет ГРАНИЦУ СЕГМЕНТА: без
// собственного макета `not-found.tsx` этой папки перехватывался бы границей
// раздела выше, и человек, открывший несуществующего пациента, попадал бы на 404
// всего списка вместо 404 карточки. Граница — и есть содержание файла.
//
// Сессию он не читает: доступ решает макет подгруппы `(staff)`, а данные —
// маршрут API. Третья проверка добавила бы круг и ничего не защитила.
export default function PatientLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
