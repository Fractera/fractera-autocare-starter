// ОДНА РАЗМЕТКА НА ОБЕ ВЕРСИИ заголовка — покой и мерцание. Файл существует ровно ради
// того, чтобы подмена была 1:1: разъедься классы, и первый экран дёрнется на глазах.
//
// 🔒 СТИЛЬ БЕРЁТСЯ У ПРИМИТИВА, А НЕ ПЕРЕПИСЫВАЕТСЯ. `H1_STYLE` в
// `components/ui/typography.tsx` намеренно НЕ экспортирован: шкала в проекте одна, и
// вторая копия её классов разошлась бы при первой же правке размеров. Поэтому близнец —
// это сам примитив `H1`, а мерцающая версия получает его классы через `class` уже
// отрисованного близнеца... нет: она получает ИМЕНА КЛАССОВ, вычисленные тем же
// примитивом, — см. `TITLE_CLASS` ниже.

/**
 * Классы заголовка первого экрана.
 *
 * Собраны здесь потому, что `Shimmer` — чужой компонент: он принимает `className` и не
 * умеет быть нашим `H1`. Это единственное место, где классы шкалы повторены, и повторены
 * они ОДНОЙ строкой, которую видно целиком.
 *
 * ⚠️ Разойдётся с `H1_STYLE` — разойдётся и подмена. Меняя шкалу заголовков, править обе.
 */
export const H1_STYLE =
  "font-serif text-[length:var(--fs-h1)] font-bold leading-tight tracking-tight md:text-[length:var(--fs-h1-md)] lg:text-[length:var(--fs-h1-lg)]"
